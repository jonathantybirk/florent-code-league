# Overview
This module contains the GCS store standards and mechanics

# Slot usage
Slots are dedicated to units so one unit owns one slot. In cases where there are too many units, a round-robin can function as fallback, especially for launchers and turrets as they are less important than