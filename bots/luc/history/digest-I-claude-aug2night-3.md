# Digest I — Claude Code sessions, night of 2026-08-02 through 2026-08-03 (evening)

> **Measured before the Aug 4 turret patch (fcode ≤ 2.3.3).** Everything below was
> measured when turrets were stronger. The 2026-08-04 balance pass (fcode 2.3.4)
> changed the Gunner to 25 HP (was 40), 20 Ti (was 10), +20% cost scaling (was
> +10%), 7 damage (was 10) and 4 ammo per shot (was 2), and the Sentinel to 40 HP
> (was 30) on a 2-round reload (was 3). That balance pass is the only rules change
> in 2.3.4, so conclusions here that do not turn on turret strength still stand —
> but every turret-heavy number needs re-measuring before it is trusted again.

Chunk brief said "night of 2026-08-02 through midday 2026-08-03"; the assigned files actually span
2026-08-01T11:17Z → 2026-08-03T18:50Z. All timestamps below are UTC as recorded in the transcripts.
Sessions are ordered by start time. The giant session (1) begins the morning of Aug 1 and runs through
the night of Aug 2; its later two-thirds are the heart of this chunk.

---

## Session 1 — cb39470f-3145-4fcb-83b9-0918bb678350 (8.6 MB)
**Span:** 2026-08-01T11:17:41Z → 2026-08-02T22:11:29Z (two /compact events inside: ~16:51Z Aug 1 and ~10:49Z Aug 2).
**Goal (evolving):** build the entire tournament/rating infrastructure from scratch, activate a live CI ladder, then pivot to bot development (tempest_reinforcements → vigil → vigil_reinforcements).

### Arc A (Aug 1, 11:17–14:30) — tournament harness built in one day
- Initial request: copy paper `1806.02643v2.pdf` (Balduzzi et al., "Re-evaluating Evaluation", NeurIPS 2018) into `articles/`; build infra for two bots to compete on all maps with role switch, results to CSV; rank by the transitive part of mElo with Nash averaging; bots pinned by **name AND commit**; set up DTU HPC submission (scrape DTU HPC docs into `docs/hpc/`).
- Maps: 21 official + 82 of Jon's generated (24 random + 30 representative + 28 stress), kept separate (`maps/generated/`).
- **The paper's own SGD (appendix E, `eta_r=16`, 16:1 learning-rate ratio) failed** — it lost to plain Elo on rock-paper-scissors because `C` can't grow off its init. Replaced with the closed-form Schur decomposition the paper recommends in §F.3 (`r = div(A)`, then real Schur of `rot(A)`). `C ⊥ r` proved unachievable alongside exact reconstruction; tested the Hodge property (cyclic term has zero divergence) instead.
- HPC facts learned: `MAX_JOB_ARRAY_SIZE = 1000` (undocumented by DTU); `module`/`bsub` absent from non-interactive shells (wrap in `bash -lc`); `gbar.sh` reads unset `$DT` and dies under `set -u`; LSF pending-job threshold ~4000 causes `bsub` to back off 60s at a time; per-user slot cap ~100–120.
- Match cost corrected: first claimed 1.3s/match (one lucky 41-turn game); real over 14,465 results: **mean 8.8s, median 6.0s (median turns = 1000), p99 39s, max 63.9s; ~58 core-hours total**. Lucas: *"No you said worst case was 21 min so set walltime to 22min"* → `walltime = "22"` plus a `check_walltime()` guard (`chunk × 66s ≤ walltime`).
- Lucas directives: chunk default 20 ("Don't cancel but set the default to 20 going forward"); *"Well all submissions should ignore repeat matches as a default"* (skip-completed default, worklist-file chunking); *"I don't think the starter bots are very interesting, ignore them"*.
- **jon-full: 23,562 matches, zero errors.** Top: `v233_h@9713344` and `vanguard@9713344` identical to 4 decimals (mElo 3.1765, Elo 552, Nash 0.5/0.5 each) — behavioural duplicates (21/42 h2h, 0 of 32 opponents differing; only diff a DEBUG flag line). intransitivity 0.220; mElo_2k Frobenius fit 2.03 vs Elo 2.92.
- Challenger mode (`--vs`): Jon's `da3fd8a` push = 8 real challengers (`.py`-hash only; an added `BOT_VERSION.toml` had made all 34 bots look changed). 12,600 matches instead of 36,162.
- **Pooled 42-bot result (36,162 matches): `tempest@da3fd8a` ranked #11 by mElo but sole Nash core agent at p=1.0000 — it beat all 41 other bots head-to-head.** mElo is a mean of log-odds and rewards crushing weak `vg_*` ancestors. jonbot new-vs-old 41/42 (+516 Elo, #36→#14); undertow 27/42 (#10→#2); vanguard 21/42 = literally unchanged.
- Duplicate detection built (`tournament/duplicates.py`): behavioural (≥3 shared opponents, even head-to-head |rate−0.5|≤tol, matching records vs every shared opponent, connected components) + code-hash. Four duplicate groups found; the 4-way vanguard group is only 2-way by code hash. Plan-time pruning (`--dedupe-from`) saves 14.6% per challenger. Ratings deliberately keep every copy (Nash empirically invariant: collapsing 42→36 leaves support identical; mElo shifts strong bots ~+0.2 logits and reorders 13 of 36).
- `AGENTS.md` written at repo root (docs/ is sourced-material-only per Jon's convention); every command in it executed before committing. Pushed to `x/tournament` (`2b043a8`, `c0076f8`, `a1ebe68`, `459f145`, `4e16825`).

### Arc B (Aug 1 16:59 – Aug 2 02:46) — mass evals, maxent verification, CI activation, the SSH incident
- "New code" eval (`new-code-fdc1e1f`): 32,088 matches, hash-scan over 102 commits/5 branches → 131 distinct code hashes, 14 new competitive bots. Result: **`tempest_oracle_ferry@fdc1e1f` new sole Nash core agent (p=1.0), beating all 57 distinct bots**; best league-legal newcomer `undertow@fdc1e1f` #8. All three `claude_challenger_*` blow the 10 ms limit (max ~12.5 ms).
- Elias eval: Lucas rejected the flat 59-bot grid (*"Not all Elias' bots are significantly different, take a few snapshots… up to like 7 bots"*); Elias himself deleted the 64-variant `cand/` grid mid-conversation (`ecd4d24`). Seven lineage snapshots showed a clean monotonic improvement curve: `autistimusprime@ecd4d24` 82.3%/+1.6736 (#16) down to `e9cand` 34.9%/−1.0997. **34,983-match run: new top bot `vanguard_oracle@902be71` (83.3%, 57% Nash mass); Nash core = {vanguard_oracle 0.575, tempest_oracle_ferry 0.305, autistimusprime 0.120} — a perfect rock-paper-scissors cycle** (0.714/0.548/0.619 edges). Lucas's `tempest_reinforcements@87724ec` #28 (68.0%) and over the 10 ms budget at p75.
- Stale-array incident: 5 leftover arrays from a discarded 245,700-match plan were racing the real run (schedule.jsonl rewritten under them → `IndexError`); Lucas authorized `bkill`; nothing lost (content-addressed match_ids).
- Maxent Nash verified beyond convergence: antisymmetry 1.8e-15, feasibility +2.0e-09, `H(p*) = 0.935230` reproduced by an independent entropy-dual solver to 6.1e-10, 12,000 feasible perturbations gain exactly 0. The real risk was 4 never-played pairs imputed as 0 — forced to ±4.44 both ways, support unchanged.
- **Lucas (verbatim, standing rule): "NEVER silently ignore missing data. Fix it."** Fix: played the 4 missing pairs locally (435 matches, matrix complete at 131,586); removed the `if coverage < 0.999` warning threshold; `Ratings.missing_pairs`/`.complete` as data; unplayed pairs named in output.
- Nash-mass pedagogy: mass is **not** a quality score (duplicating vanguard_oracle halved its mass 0.5746 → 0.2878+0.2868; core agents all have Nash average n=0). Lucas's "lower percentage is better" overcorrection also refuted: no monotone relation in either direction.
- **CI built and activated** (from existing `automation.py` skeleton): multi-source discovery (`x/jon:bots/jon`, `x/luc:bots/luc`, `elias_dev:bots/elias`); ledger **derived from `runs/*/matches.csv`** instead of a JSON cache after Lucas's *"Can't we just pull the ledger directly from our actual data in the repo?"* (cache had 48 vs 81 real implementations — 17 bots would have been re-run); stable tid = hash of implementations under test (previous tid moved when any branch pushed, stranding half-finished runs); `unfinished_runs()` in-flight tracking; lock kept but narrowed to discover→plan→submit after Lucas approved the two-phase non-blocking tick ("Yes please, the last part sounds good").
- *"Check out finalise yourself. Test out the CI and then activate"* → review found `finalise()` broken (2 NameErrors, pooled only 49-bot canonical run, StopIteration risk); rewrite; end-to-end test then caught a fifth bug — **scheduler and rater used different duplicate-representative choosers**, leaving 2 never-played pairs; unified into one `canonical_field()`. The "never silently ignore missing data" guard is what caught it. Timer enabled: `botrankings-evaluator.timer`, every 2 min, dedicated worktree `~/projects/florent-code-league-ci` on `x/tournament`.
- Self-update + publish hygiene: each tick `--ff-only` merges `origin/x/tournament` gated on pytest passing, exits so next tick runs new code; site deploy refuses when the site repo is dirty outside `public/botrankings/data` (Codex's uncommitted compliance-toggle work in `BotRankings.tsx` was being silently built and deployed). Compliance toggle ("Include over-time strategies") already existed as a genuine recompute (61 compliant bots/76,860 matches vs 73/110,376) but the UI bundle never went live (`wrangler deploy` reported success while live served the old `BotRankings.Dpq5irqO.js` — never explained).
- **The SSH incident:** Claude ran `ssh -O exit dtu` at 02:00:08 to hand the socket to a new `dtu-ssh-master.service`; DTU became unreachable 78s later. Lucas: *"I find it interesting that the connection to dtu failed the second you started messing with it. What did you do?"* Claude owned it (killed the only working master; `ssh -O check` only checks the local socket), then corrected an evidence error (had claimed "no NetworkManager events" from a log it couldn't read). Diagnosis: the router stopped advertising IPv6 (both interfaces link-local only, no RA), and DTU's login node refuses IPv4 :22 from off-campus — IPv6-only. Recovery via the Cisco VPN (`cscotun0`); evaluator resumed at 02:46:34 with all 3,069 results of `auto-c4423c4b3d59`; nothing lost. Router IPv6 remained broken (masked by VPN).

### Arc C (Aug 2, 10:49–22:11) — the bot-development day (core of this chunk)
- **CI data push** (Lucas: keep `x/tournament` remote updated so friends can analyze): `_push_data` runs once per tick in a `finally`, stages `tournament/runs/`, refuses off-branch or with a dirty tree outside runs/, rebase-retry, never force. First live push `eafa9162a` (120,683 lines).
- CI had already picked up `x/luc@65ddc55` unattended: **`tempest_reinforcements@65ddc55` rank 1 on mElo (+2.457) and sole Nash core agent (p*=1.0000)** over a 75-bot, 116,550-match field. Previous head `025dd51` rank 7 (+1.946).
- Replay vs `mistral_fast@26b5aa1` (joint-hardest matchup with `mistral@ba943b9`, both 23.0/42 = 54.8%): mistral_fast rushes (turn-22 core kill, 0 mined). Lucas ordered: first Launcher on the enemy-approach lane; one fewer attacker; extra builder to the Launcher wall. Result 29.0/42 (69.0%) vs both mistrals, 31.0/42 vs vanguard_oracle; committed `3d5ab38bf`, pushed `38f9c239f` (with radius-2 ring, below).
- **The cost-scale revelation** (Lucas: "It says it needs 51 titanium for the third launcher… how can it be so expensive?"): `effective_cost = base_cost × scale_factor`; scale is a live census (+20%/builder, +10%/turret or launcher, +5%/harvester, +1%/conveyor·splitter·barrier, refunded on destruction). Six builders = 120 of the 157 extra points; Launcher 20 Ti → 51 at 257%. A sixth unplanned builder came from `REINFORCEMENT_TITANIUM_THRESHOLD = 120`.
- Lucas's redesign (one attacker, one ring-defence builder, one economy builder): **cutting six builders to three was worth +8/+8/+7 games alone** (scale settles 152–157% not 267%; launcher 30 Ti not 51; bank 211–241 Ti not 18). A `NameError` on `RING_EDGE_MARGIN` accidentally provided the clean control. Ring at radius 1 strangled the conveyor lane into the Core (−4/−4/−1); radius 2 recovered it: totals 89 → 104 → 113 (no ring) → 112 (r=2) /126. r=2 vs no-ring split by opponent type: +1 vs both rush bots, −3 vs the economic vanguard_oracle. Pushed as `38f9c239f`.
- **vigil born** (Lucas cited online match `a2db7209-…`: the meta = turrets to destroy units, rotate idle turrets toward nearest enemy, builders counter blocking launchers; Lucas's own idea: thin out turrets in quiet areas): fork of tempest_reinforcements, commit `18b749dc7`. Empirically established that **a turret can self-destruct** (and `ct.destroy()`/self-destruct refunds scale). Ablation: rotation helps, thinning neutral (fires too late; games end ~turn 30), field engagement negative (−3 at cap 3) → shipped `MAX_FIELD_GUNNERS = 0`. vigil 39/39/38 vs tempest's 38/38/36.
- **Seat-split (gold/silver) analysis** (Lucas asked if vigil is unstable): engine perfectly deterministic at `--tle 0` (6 seeds identical winners AND turn counts). Field baseline: seat A wins 48.69% over 166,278 matches; per-bot gaps to ±15pp (vanguard_oracle@75591b7 −14.9). vigil's gap −0.43pp (94.20% vs 94.63% over 3,276 cluster matches); zero of 78 opponent pairings exceed |z|>2. **vigil has the lowest seat-split rate of all 96 bots (8.0%)**; the 6-builder ancestor was 20.0% — the 3-builder+ring opening more than halved seat sensitivity. Splits concentrate where matchups are close, not where maps are biased.
- Core seal (Lucas: block enemy builders from every tile that can reach tiles-that-reach-our-core): barriers (3 Ti, +1% scale, block LOS) not launchers; flood-fill-proven 0 leaks; 12 barriers corner / 32 open. But games end ~turn 30 and only ~1 barrier/game gets laid → −3 games; kept behind `SEAL_TITANIUM_RESERVE`, later `CORE_SEAL_ENABLED = False`. Committed `2b6606189`; launcher-breaker (shoot enemy launchers blocking the route, 2,338 detections/84 games) `bf9b3e374`.
- Sharpening after Lucas's "It's not a strategic problem, it's 100% definitely because we're not able to build the launchers properly": (1) breaker had targeted by convenience — of 11,717 launchers seen during path failures **only 209 (1.8%) actually blocked the route**; on-route became a ranking key, not a filter (filtering cost 2 games — a dead launcher is always worth something); (2) **1,067 candidate turret sites would have walled our own attacker in** → `_keeps_route_open`; (3) biggest: the ring builder's fall-through created a second economy builder fighting over a single `CLAIM_SLOTS` ore reservation — garrison instead, +5 games (nearly mis-attributed to "seal off" which had actually made the builder idle). Final `e22eda8d6`: 40/40/38 = 118/126. **Pantheon finding: Pantheon's ladder wins run 162, 310, 321, 409 turns; the local pool resolves in ~30 — the benchmark is the wrong regime for defensive features.** Ladder: Pantheon 1925, clear of second by 143.
- Robustness pass (Lucas: builders that can genuinely do nothing should self-destruct; fix broken conveyors with priority; blocked bots build a launcher after ~5 rounds; defense must cover distance-4): diagnostics showed **1,232 idle builder-rounds/21 games, 1,031 of them the garrisoned ring builder** (worst streaks 984/983/982 rounds) — the +5-game garrison was itself the idleness hole. Engine fact discovered: **store writes are invisible to other units until the next round** (a same-round bitmask census is unimplementable) → affordability-based write-off guard instead. Write-off bug chain: `_step` runs many times per turn (25 path failures in round 1 → builder id 3 self-destructed on frame 4); then `path_failures` never reset on progress (healthy builders suiciding at round ~91) — both fixed (once per round, cleared on progress).
- Distance-4 launcher cover: Lucas's geometry implemented but **launchers are the wrong material** — 20/42 vs tempest and 22/42 vs prospect against the radius-2 compass ring's 26/27. Shipped off behind `RING_COVER_SHELL`; commit `2956db378` (four robustness fixes worth +1 over e22: 26.0/42 vs tempest_reinforcements, 27.0/42 vs prospect). Lucas corrected the benchmark: *"Vigil's actual best counters are tempest_reinforcements and prospect now."* (Session limit pause 16:07→19:20Z.)
- **Compliance regression was self-inflicted**: `_keeps_route_open` (from e22) ran two BFS per candidate site, up to 81 BFS/turn → max 14.55 ms, 0.6% of turns over 10 ms. Fix (route cached per round; off-route candidates cleared without search): p99 7.39→1.24 ms, max →2.39 ms, 0 over; behaviour identical; at `--tle 10` wins the same 13/21 as at `--tle 0`. Pushed `e267eeb41`.
- Ring geometry saga (Lucas: "The launchers are NOT being built correctly!… draw me how you imagine the launchers to be placed"): three successive corrections — Chebyshev square ring (wrong) → disc radius ~4.6 (wrong) → **Lucas's ray-aligned danger zone: a gunner threatens the core only from tiles aligned with a core tile, 3 out cardinally, 2 out diagonally (the "plus with 2×2 corner blocks")** — 9 launchers cover the 24-tile rim, 0 leaks; then reachability pruning (flood from map border): corner core needs **3 launchers on sweden, exactly as Lucas said**; then the "2 of 3" hole — a ring site occupied by our own conveyor was silently marked done → re-solve cover around taken tiles; plan also re-solves as terrain knowledge grows (was cached once at round 3).
- **New dev cycle decreed by Lucas (20:57Z):** "I say improvements, you do them and open a game for me to watch against a hard opponent (ragnarok or the current best vigil, only offline of course)… Opening a game should be one command." Also: no commits until asked.
- Rapid-fire fixes in that loop, each vs ragnarok on sweden at `--tle 10`: escape launcher walks up to 6 tiles to build on-ring (0/8 → 3/6 on-ring); orphaned-ring fix (fixed `LAUNCHER_BUILDER_INDEX=2` meant a dead ring builder's replacement became an attacker — replacements now adopt ring gaps); **launch-request redesign: the request carried only a 4-bit compass bearing and the launcher maximised projection along it, routinely throwing builders *farther from* their target — now carries the real destination in 11 bits and requires the landing to be strictly closer**; then the decisive units fix — builders move cardinally, so a 1-tile-diagonal throw saves 2 walked steps but costs `LAUNCH_OVERHEAD_ROUNDS = 2` (store-write visibility + waiting) — measuring in Manhattan steps and requiring saving > overhead **fixed the chronic 0-mined sweden games: 9500 mined vs 10** (economy builder had been ferrying itself a tile at a time). Conveyor reuse per Lucas's spec (join any friendly line if downstream terminal harvester count stays ≤4; routing-optimisation ideas left as a comment); attacker uses remembered friendly launchers (`p.friendly_launchers`) with hop cost `walk + 2 + max(0, dist−LAUNCH_REACH 5)`; turret targeting value-ranked (`TARGET_VALUE`: Core 100, Gunner 60, Sentinel 55, Launcher 45, Builder 40) — **`CORE` was absent from the old priority table**; gunners lead moving targets because `rotate()` sets action cooldown 1 (turn-then-fire is structurally impossible); scaled patience (≤2-step detour: go; long way exists: wait 1; no way: wait 3); demolish own blocking turrets (no action cooldown, scale refunded); enemy fire as path cost, not a wall (Lucas: "No only if they'd actually die… sometimes it's better to just tank it") — gunner 10 dmg/round r²13 blocked by walls, sentinel 6/round r²32 unblockable, blocked only if the trip would kill.
- Process honesty note: one reported game had not actually included the edit (script failed silently; identical numbers were the tell) — confessed and re-verified.
- **Session end (22:08–22:11):** Lucas: "Commit and push, call it vigil_reinforcements." `bots/luc/vigil_reinforcements/` created as a copy of the working vigil (BOT_VERSION.toml: source_commit e267eeb41, status "unfair", variant "threat-boundary-ring, launch-tempo, damage-weighted-pathing"); final timing 2790 turns, median 0.26 / p99 1.79 / max 2.44 ms, 0 over; head-to-head **vs vigil 22.0/42 (52.4%), vs ragnarok 16.0/42 (38.1%), vs tempest_reinforcements 18.0/42 (42.9%)**. The `git commit` tool call failed ("claude-sonnet-5[1m] temporarily unavailable" for the safety classifier) and the session hit its limit — **the commit+push of vigil_reinforcements did NOT complete in this session.**

**Names introduced:** `tempest_reinforcements` (Lucas's bot; per its BOT_VERSION.toml a fork of Jon's unfair `tempest_oracle_ferry`, atlas-oracle line); `vigil` (Lucas's meta-capture fork of tempest_reinforcements: turret rotation/leading, launcher-breaker); `vigil_reinforcements` (fork of vigil with threat-boundary ring, launch tempo, damage-weighted pathing); `ragnarok` (Lucas bot that appeared mid-session from a parallel agent; the hard sparring partner); `prospect`, `prospect_rushonly` (Lucas bots from a parallel commit `1b5f3d1b8`); `mistral_fast`/`mistral` (rush bots, tempest_reinforcements's joint-hardest matchups); `Pantheon` (external ladder leader, 1925 Elo, long-game style); `autistimusprime` (Elias's published entrant, Nash core member); `vanguard_oracle`, `tempest_oracle_ferry`, `casemate`, `v233_i` (Jon bots); scale factor (live cost multiplier census); gold/silver = seat A/B.

---

## Session 2 — fc35d659-a85c-4a23-8ff6-a81d6f617e7f (226 KB)
**Span:** 2026-08-02T20:30:32Z → 2026-08-02T22:18Z. Ran in parallel with the tail of Session 1. Templated "How to do it" prompts (SDK-style replay-opening workflow): sample rated rows from `matches-distinct.csv` in the CI worktree, re-run with pinned staged trees, open viewers.
- **ragnarok@34f775b vs ragnarok_fair@34f775b on sweden: ragnarok_fair swept both orders** — core destruction turn 103 as Silver; titanium tiebreak 10698 vs 5000 at turn 1000 as Gold. Flag: ragnarok sat on 5000 Ti for 1000 turns without converting (stall, not a close loss).
- **ragnarok@34f775b vs tempest_reinforcements@38f9c23 on sweden: ragnarok won both orders** (turns 108 and 79, core destruction).
- Provenance flag: `tempest_reinforcements@38f9c23`'s staged `BOT_VERSION.toml` reads `status = "unfair"`, `source_path = bots/jon/unfair/tempest_oracle_ferry`, `variant = oracle-reinforcements-launcher-fallback`, and its `source_commit = 7196487…` does not match the `38f9c23` directory pin — pin metadata inconsistency noted.
- **10 random rated matches for `vigil@e267eeb` (pool 3,654 rows in `auto-60bd21cbccad`): vigil won all 10**, 7 by core destruction inside 50 turns (opponents incl. tempest_frontier, siege_v1, tempest_ferry, riptide, e9cand, tempest_reinforcements@3d5ab38, v233_c, vg_v4, v233_d, claude_challenger_2). Caveat raised: sweep reads as opponents collapsing early; compare vs recorded CSV winners to rule out staging issues. 14 viewer servers left running.

---

## Session 3 — cb42e2ff-62ba-45d0-9a55-000d0d75619b (424 KB)
**Span:** 2026-08-03T09:20:22Z → 2026-08-03T11:13:59Z.
**Goal:** get Viktor's branch watched by the CI; ended up documenting where the CI actually runs.
- Q: "Are we watching viktors branch as well?" → No; watched set was `x/jon`, `x/luc`, `elias_dev`. Lucas: "Watch viktors branch as well."
- Added `Source("viktor", "bots", excludes=("bots/tester", "bots/test", "bots/test/*", "bots/starter"))` — Lucas corrections in sequence: "no he will make on called test" (glob), then "The exact names please, and note that test will be a directory with many bots". Discovery found exactly one entrant: `green@97e1e83` at `bots/green`. Pushed `94aa1018a`; 133 tests pass.
- Lucas: "you said some of his bots were evaluated?" — corrected: none evaluated, discovery ≠ evaluation.
- **Root-cause discovery (after Lucas pointed to conversation history): the live CI is `botrankings-evaluator.timer`, a systemd *user* timer on this machine every 2 min, and the installed unit passes explicit `--source` flags that replace `DEFAULT_SOURCES` wholesale — so the Python edit `94aa1018a` was a no-op for the live ladder.** Fixed by adding `--source viktor:bots:bots/tester,bots/test,bots/test/*,bots/starter` to the unit + `daemon-reload`. Pushed `8447875b9`.
- Twist: Viktor pushed `ab8ceaf58` "Added a 'test' folder for non-ready bots" mid-task, moving `green` under `bots/test/` — so discovery now returns **zero** Viktor entrants until he promotes one (spec working as written).
- Also noticed: ~10 stale `fcode run` processes wedged on vigil vs ragnarok from `auto-10d581bed771`; six tracked files in the CI worktree modified by another agent.
- Documentation per Lucas's instruction ("document your findings… such that you won't be confused again. then update my personal claude.md… I work across all branches"): `tournament/README.md` "Where the CI actually runs" section; pointer comment on `DEFAULT_SOURCES`; unit file resynced (it had also drifted on `--self-update-branch`); **`CLAUDE.local.md` created** (branch-per-task workflow) — chosen over the tracked shared `CLAUDE.md`. Lucas: "Gitignore it please" → put in `.git/info/exclude` (applies to all worktrees, no commit; the main worktree was on branch `viktor` at that moment).

---

## Session 4 — e677e0c8-8475-4dca-945a-80e9517e8638 (205 KB)
**Span:** 2026-08-03T09:21:15Z → 2026-08-03T10:31Z (parallel with Session 3).
**Goal:** read-only analysis: "Across all our bots, especially those that are best (vigil, ragnarok, tempest, vanguard) how do we use the global communication store."
- All four treat the 16 store slots as a statically partitioned register file; `pack_pos(x,y) = 1 + x*32 + y` with 0 = empty; the one-round write delay is used deliberately (slots double as locks).
- Five shared protocols documented: (1) slot-0 ticket dispenser assigning permanent `builder_index` → role; (2) ore claims where the value is the key (dead builder leaves a stale claim, never corrupts others); (3) construction lock as a lease `owner | (expires << 2)` with 20-round expiry (crash-tolerant); (4) **symmetry inference as a free CRDT** — monotonic OR of rejection bitmasks across two slots disproves mirror-x/mirror-y/180° enemy-core hypotheses, winner published to `SLOT_ENEMY_CORE`, scouts re-split by `index % candidates`; (5) launch handshake — vanguard minimal (one ferry at a time), vigil/ragnarok a request/reply channel with per-builder slots, packing `unit_id << 4 | direction`, and a rejection reply (bit 31 + `pack_pos` of the blocking enemy Launcher) that feeds sensor data back to the passenger.
- Ambient flags: `SLOT_OWN_CORE`, core-damaged latch, vanguard's `SLOT_HOME_UNDER_FIRE` with explicit hysteresis (clear only at full HP — a never-clearing latch once made every attacker a permanent repairman); vigil/ragnarok `SLOT_BUILDER_HEARTBEAT` (Core detects "no living Builder" and respawns past cap); vanguard's `SLOT_ECON_LINES` counts lines *built*, not alive.
- Practical note: `bots/luc/*` in the working tree held only `__pycache__` (sources live on `x/luc`); tempest/vanguard read from staged run copies.

---

## Session 5 — 0e42e08a-b272-4afa-8519-3305e65056b6 (131 KB)
**Span:** 2026-08-03T16:09:55Z → 2026-08-03T16:16:22Z.
**Goal:** "did gobbleglitch find any glitches to exploit" — summarising Elias's exploit-hunt (`llm-slop-analysis/elias/exploits/` on `origin/elias_dev`, eight reports + SYNTHESIS.md; bot at `bots/elias/unfair/gobbleglitch/`).
- **The namesake "gobble lock" was refuted** — trapping enemy builders ("989 gobbles / 4 breaches") was an unsealed-cell artifact; `holdmax=15` = 30 HP barrier ÷ 2 dmg/chew; a sealed cell gives `gob=1 esc=0`.
- Findings (Claude then reclassified them under Lucas's "so no glitches found?"):
  - **Sentinels shoot through walls/buildings/bodies** — documented rule ("never blocked by walls or units"), just under-used; a Sentinel behind a two-tile wall killed a 500 HP Core at turn 84 (500÷18 = 28 shots × cooldown 3, exact); +54% firing positions on the enemy Core.
  - **Cost scale is a live census with full immediate refund** (1224 checks, 0 mismatches); attack/heal/rotate/`convert_ammo` are flat at any scale.
  - **The one genuine undocumented engine detail: turn order is ascending global entity id across both teams** (same bot won 4/4 as id 3, lost 4/4 as id 4) — the mechanism behind the A-side mirror bias and the Launcher→Gunner same-round chain gate.
  - Rebuild-tanking: 201 rounds of gunner fire absorbed at 1.01 Ti/round, Core untouched.
  - Turn-1000 tiebreak ladder: `titanium_collected` → live `harvesters` → `titanium_stored` → coinflip; passive income scores zero.
  - Lying predicates (API defects that hurt *you*, not levers): `is_tile_empty` True under a builder, `can_act()` True after a move while every action raises, `get_nearby_units()` includes buildings, store slots raise `OverflowError` not `GameError`, `ct.self_destruct()` on your own Core is legal and loses.
- What shipped in gobbleglitch: Sentinel third pass, Launcher retirement on the scale refund, guard Launcher, body-aware occupancy (a fix to Elias's own bug: a parked enemy body passed `_building_at`, costing gunners in 41 of 42 games). Known-map win 80.8% → ~88%, unseen 52.1% → 66.0%.
- Biggest non-glitch finding: **the atlas map table was worth one bit** — which symmetry candidate holds the enemy Core; farthest-first guessing recovers most of it. `ragnarok` already absorbed that plus rebuild-tanking and the `get_nearby_units` filter (per `benchmarks/ANALYSIS.md:214`).
- Honest bottom line, conceded on Lucas's push-back: an eight-agent exploit hunt found **no exploitable engine bugs** — it produced a corrected mechanics register (14 documentation errors) wearing an exploit-hunt name.

---

## Session 6 — b908c199-dfcd-434c-8cbf-40a23b9a41aa (39 KB)
**Span:** 2026-08-03T18:50:16Z → 18:50:35Z (single exchange).
**Goal:** "What is the status of the CI for eval?"
- `botrankings-evaluator.timer` active, firing every 2 min (last run 20:49:16 local, exit 0), watching `x/jon`, `x/luc`, `elias_dev`, `viktor`.
- **Two new Lucas bots queued but not evaluated: `aegis@5709fbf` and `warden_walk@5709fbf`, deferred "until 99 bot(s) under test have landed."** The 99-bot under-test backlog and the ledger (stuck at 114 rated implementations) had not moved across three consecutive runs (20:43/20:46/20:49) — a stalled match queue, not a broken timer. Offered to dig into where the 99 pending bots are tracked; session ends there (open thread).

---

## Chunk synthesis

**Load-bearing insights (with evidence):**

1. **The entire rating stack — mElo transitive ranking + maxent Nash averaging, commit-pinned bots, DTU HPC job arrays — was built, validated, and made a live 2-minute CI in ~36 hours** (Session 1, Aug 1 11:17 → Aug 2 01:30 activation). Determinism verified byte-identical local↔cluster (36/36 matches, 7 fields).
2. **The paper's own mElo SGD does not work** (loses to Elo on RPS; 16:1 learning-rate ratio starves `C`); the closed-form §F.3 Schur construction replaced it. (Session 1 Arc A.)
3. **Nash vs mElo disagreement is real and Nash is the truth-teller**: tempest@da3fd8a #11 by mElo yet beat all 41 bots (sole core agent); mElo rewards crushing weak ancestors. Later the core became a measured rock-paper-scissors triangle (vanguard_oracle 0.575 / tempest_oracle_ferry 0.305 / autistimusprime 0.120), and duplication-halving proved Nash mass is not a merit score. (Arc A/B.)
4. **Duplicates are endemic and behaviourally detectable** (21/42 signature; 4 groups in Jon's roster; the 4-way vanguard group is 2-way by code hash); dedupe at plan time saves compute forever. (Arc A.)
5. **Lucas's standing rule "NEVER silently ignore missing data"** directly caught the scheduler/rater representative-mismatch bug that would have published two fabricated results. Missing pairs are data (`Ratings.missing_pairs`), not a thresholded warning. (Arc B.)
6. **Derive state from the repo's own results, never cache**: the JSON ledger drifted 48 vs 81 implementations and would have re-burned ~36k matches; `derive_ledger()` from `matches.csv` fixed it structurally. Same theme: stable tid from work-content hash, in-flight tracking, self-update gated on pytest, deploy-only-committed-code. (Arc B.)
7. **The cost scale factor is the game's central economic mechanic**: a live census (+20% builder / +10% turret / +5% harvester / +1% conveyor-class, fully refunded on destruction). Cutting the opening from six builders to three was worth +23 games/126 and halved seat sensitivity (split rate 20.0% → 8.0%, best of 96 bots). (Arc C; corroborated by gobbleglitch's 1224-check measurement, Session 5.)
8. **The engine is perfectly deterministic at `--tle 0`** (seed changes nothing), and **turn order is ascending global entity id across both teams** — the real source of seat bias (field: seat A 48.69% over 166,278 matches). (Arc C + Session 5.)
9. **Store writes are invisible to other units until the next round** — kills same-round censuses, but is exploited deliberately (slots as locks, CRDT symmetry inference, launch request/reply). (Arc C discovery; Session 4 catalogue.)
10. **Launcher ferrying was the hidden economy-killer**: bearing-only requests + radial "strictly closer" acceptance made builders take 1-tile-diagonal throws costing 2 overhead rounds each; fixing to real-destination + Manhattan-step gain > overhead took sweden from chronic 0 mined to 9500 mined. (Arc C.)
11. **The local benchmark regime is wrong for the ladder**: Pantheon (ladder #1, 1925, +143 clear) wins in 162–409 turns; the mistral-pool games resolve in ~30, so defensive features (core seal, turret thinning, field gunners) measure neutral-to-negative locally. vigil-vs-tempest (mean 119 turns) proposed as the long-game proxy. (Arc C.)
12. **Lucas's ray-aligned danger-zone geometry is correct and minimal**: gunners threaten the core only from aligned tiles (3 cardinal / 2 diagonal), rim coverage needs 9 launchers open-field and only 3 on sweden's corner core after reachability pruning — but launchers are the wrong material for full-shell denial (barriers at 3 Ti / +1% are). (Arc C.)
13. **Measured-ablation discipline repeatedly overturned intuition**: the ring at r=1 strangled the own conveyor lane; garrison (+5 games) was simultaneously the 1,031-idle-round robustness hole; the launcher-breaker's broad "wrong" trigger outscored the correct filter; three accidental controls (NameError ring, idle-not-seal, edit-script no-op) were each caught because numbers were checked against known baselines.
14. **The live CI's watched branches come from `--source` flags in the installed systemd unit, which override `DEFAULT_SOURCES` in code** — editing the Python is a no-op for the ladder. Documented in `tournament/README.md`; `CLAUDE.local.md` (branch-per-task workflow) created and excluded via `.git/info/exclude`. (Session 3.)
15. **gobbleglitch found no exploitable engine bugs** — its namesake was refuted; the yield was a corrected mechanics register (sentinel wallshot is documented; tiebreak ladder; scale arithmetic; entity-id turn order the only genuine undocumented detail) plus "the atlas is worth one bit," already absorbed into ragnarok. (Session 5.)
16. **Bot lineage at chunk close**: tempest_reinforcements (fork of Jon's unfair tempest_oracle_ferry) → vigil (meta capture: rotation/leading turrets, launcher-breaker) → vigil_reinforcements (threat-boundary ring, launch tempo, damage-weighted pathing; status "unfair"). vigil@e267eeb swept 10/10 sampled rated matches; ragnarok emerged from a parallel agent as the strongest sparring partner (beat tempest_reinforcements both orders; loses to ragnarok_fair both orders on sweden).

**Loose ends open at chunk close:**
- **vigil_reinforcements' commit+push never completed** (Bash safety-classifier outage + session limit at 22:11Z Aug 2) — the directory existed uncommitted with vigil_reinforcements beating vigil only 22.0/42 and losing to ragnarok 16.0/42.
- **CI stalled behind a 99-bot "under test" backlog** at Aug 3 18:50Z; `aegis@5709fbf` and `warden_walk@5709fbf` deferred; ledger stuck at 114 rated implementations across three ticks. Cause not yet investigated.
- ~10 stale `fcode run` processes wedged on vigil-vs-ragnarok (`auto-10d581bed771`) consuming slots (noted Aug 3 morning).
- Viktor has zero discoverable bots (everything under the excluded `bots/test/`); `green` enters only when promoted.
- Router IPv6 still broken; DTU reachability rides on the Cisco VPN (`cscotun0`) — CI uptime tied to the tunnel.
- The unexplained `wrangler deploy` success-with-stale-bundle incident was never diagnosed.
- Sweden's "46 buildings, 0 titanium mined" economy failure was largely explained by the ferry bug, but the earlier flagged line-never-completed variant wasn't separately confirmed fixed.
- tempest_reinforcements@38f9c23 pin metadata mismatch (`source_commit 7196487` vs directory `38f9c23`) unresolved.
- 14 replay viewer servers left running from Session 2.
