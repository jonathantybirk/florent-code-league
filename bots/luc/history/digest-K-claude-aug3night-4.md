# Digest K — Claude Code sessions, night of 2026-08-03 into 2026-08-04

> **Measured before the Aug 4 turret patch (fcode ≤ 2.3.3).** Everything below was
> measured when turrets were stronger. The 2026-08-04 balance pass (fcode 2.3.4)
> changed the Gunner to 25 HP (was 40), 20 Ti (was 10), +20% cost scaling (was
> +10%), 7 damage (was 10) and 4 ammo per shot (was 2), and the Sentinel to 40 HP
> (was 30) on a 2-round reload (was 3). That balance pass is the only rules change
> in 2.3.4, so conclusions here that do not turn on turret strength still stand —
> but every turret-heavy number needs re-measuring before it is trusted again.

All timestamps are UTC (Z) as recorded in the transcripts; local time is UTC+2 (Europe/Copenhagen). Three sessions; the second and third overlap the first's tail and each other — multiple agents were running against this checkout at once, by design.

---

## Session 1: `1557230d-4541-4d57-ac3f-7100f642c88b`
**Span:** 2026-08-03T10:31:16Z → 2026-08-03T22:45:29Z (12:31 → 00:45 local). Infrastructure session: held-out map pool, Nash-core-maps metric, site changes, and a chain of harness fixes.

### Goal
Lucas (10:47Z): add a metric to botrankings for "how many maps the bot is part of the nash core", sortable; he had added secret maps under `tournament/` in the repo — gitignore them, add a README saying no one may see or use them ("purely for evaluation... on new terrain"); on the website add a dropdown for Standard maps (default) / Secret / Secret + standard, with a note that the secret pool is very probably out of the distribution of maps the florent team will make, so overall performance is hard to interpret and per-map sorting is more interpretable; per-map view allowed but render only name and size, never terrain.

### Narrative
- Assistant mapped the system: harness lives in the CI checkout `~/projects/florent-code-league-ci/tournament/`; site data built by `tournament/site_data.py` → `~/projects/portfolio/public/botrankings/`, rendered by `portfolio/src/components/BotRankings.tsx`. Nash/mElo are field-relative, so pools must be precomputed server-side: 3 pools × 4 fields = 12 ranking sets. The 10 secret maps sit at `tournament/custom_maps/` (deliberately outside `maps/` so no glob reaches them).
- Hit a permission wall for the CI and portfolio checkouts. Lucas (10:57Z): "I give you permission to do what you need to do, Also switch my branch away from viktor to luc please. and run the first secret map eval run." Also: remove the "nash recommended" tags everywhere (gold colour on the Nash column suffices) and put his explanation on the site — supplied in Danish, to be paraphrased in English: every metric depends heavily on the map pool; Nash averaging calls a bot #1 if it wins the majority of maps even if "absolut destrueret" on 40% of them; overall win rate and mElo r additionally depend on the bot pool (Nash averaging does not); the only number immune to both confounders is per-map Nash averaging.
- Built: gitignore + `tournament/custom_maps/README.md` ("a seen map is a spent map"); `secret/<name>` map labels with a bare `--maps geode` now failing instead of silently mixing pools; `nash_core_maps` / `pool_maps` per ranking row with a sortable "Core maps" column (e.g. `12 / 21`); Maps dropdown; secret terrain stripped from the JSON bundle, not just hidden; all "recommended" badges removed. Unasked-for but necessary fix: `ratings.csv`, `duplicates.csv` and the canonical field now filter to official maps only, so the first secret run cannot silently redefine the published ladder. 137 harness tests pass (4 new).
- **First secret eval run** (`secret-eval-1`): 5 LSF arrays, DTU HPC jobs 29015696–29015700, planned 83,720 rating matches (92 bots × 4,186 pairs × 10 secret maps × 2 sides; quoted in-flight as 85,652 including extras). Completed ~2.5 h wall, 100% `status=ok`, zero EXITs.
- On request, mathematics on the site typeset with KaTeX (bundled locally; all 12 formulas verified). Commits: llm-rl `be23ec6d9` → rewritten `abaeeed3c` after Lucas said "Exclude the bots/luc/NOTES.md. But otherwise fine to push"; CI `df7781c36`; portfolio `2ecb04a`. All pushed. Note rewritten verbatim per Lucas (push `2ecb04a..31904c0`; only edit: `obivously` → `obviously` typo), and the duplicate copy removed from the "How every number is calculated" panel per Lucas (`31904c0..68a1d7b`).
- **The field mistake (16:09Z):** the run was planned from `bots.toml` (92 bots) rather than the canonical rated field — 17 canonical bots were absent, and they were "the entire top of the standard ladder": `ragnarok`, `ragnarok_fair`, all six `vigil` commits, `vigil_reinforcements`, `janus`, `prospect`, `prospect_rushonly`, three `tempest_reinforcements`. The secret ladder that came back was led by `vanguard@9932bec` (core 3/10) and `autistimusprime` with Lucas's best bots absent. Gap-fill in challenger mode: by then 19 bots were missing (`valkyrie@30d0491` and another `gobbleglitch` had entered), roster 92 → 111, jobs 29021916/29021917, 31,939 matches; done with zero EXITs. Final matrix: **93 bots, all 4,278 pairs played**, 85,560 secret rating rows.
- **Structural fix behind the mistake:** `bots.toml` is a hand-refreshed cache (`discover` only runs when a human runs it) while the automation builds its field from `derive_ledger()` — minutes after the fix the ledger had 117 bots to the registry's 111. `plan` now resolves bots against registry **and** ledger, and warns (naming them) when the chosen field can't complete the canonical matrix — re-running the day's exact failing command names all 19. `site_data` gained the completeness guard `finalise` already had, per pool. Deliberately left alone (per Lucas's point about the atlas check): stored fairness tags — `is_unfair` treats a recorded `unfair` as an override the heuristic can't undo, so making them derived would delete a safety net. Commit `d2552e059`; 139 tests.
- **Nash solver bug, found and fixed** (`fa45d029c`): the site build aborted on map `pinch` (field "including over-time"). The max-entropy dual was terminating on `ftol` after 34 iterations at `max(Ap) = 1.03e-06`, a hair over the `1e-6` acceptance bound, reporting total failure — 63 iterations would reach `9e-14`. Since the dual's gradient is `-Ap`, `gtol` is the real feasibility criterion; `ftol` now sits below machine precision. Latent until the field grew from 91 to 93 bots.
- **Three-pool standings** (scratch bundle, 76-bot timing-passing field of 93 rated): Standard #1 ragnarok `79582fc` (core 10/21), Secret #1 tempest_reinforcements `65ddc55` (4/10), Combined #1 **vigil `e267eeb` (16/31)**. The held-out maps disagree with the official ones: ragnarok and valkyrie tie for first on standard and neither reaches the secret top 8; valkyrie falls to 7th combined. **`vigil@1b5f3d1` ties Nash rank 1 on the secret pool while core on 0/10 of its maps** — per-map ranks 12, 10, 36, 32, 19, 25, 28, 4, 22, 44, holding 12.8% of the equilibrium mixture with a 73% win rate. Lucas's confounder argument made concrete. Most robust bot: `vigil@e267eeb` — 4th standard, 2nd secret, 1st combined.
- The automation then published everything itself (tick `auto-f11bf027e3d4`, 18:04Z). Live standings put newcomers `warden`/`warden_walk@3318ffd` straight into the standard top. Standard/combined show 81 bots vs secret's 76 — new bots never enter the secret pool without an explicit held-out run, a growing gap by choice.
- **Stranded-results wedge** (18:53Z, "What about evaluating new bots?"): `auto-f11bf027e3d4` was 12,285/12,285 done on the cluster but **5 result files were never fetched**, so `unfinished_runs()` kept it in flight and every tick deferred `aegis`, `steward`, `warden_walk` ("99 bots under test") for about an hour. Manual fetch cleared it; next tick submitted `auto-756c8d8b3cd3` (12,537 matches). On Lucas's "yes", a recovery guard was added: when a run looks in flight, ask the cluster; if done with results missing locally, fetch instead of deferring; runs ahead of the deferral decision. Commit `55da74119`, 142 tests (three new; four pre-existing tests' `hpc.config` canary retargeted to `hpc.submit`, flagged as touching someone else's tests).
- **First-published dates** (Lucas, 19:14Z): for every bot find the first commit that published it (duplicates take the oldest in the group) and show day/month + time beside every bare commit id on the site — but not in `vigil@e267eeb`-style forms. Identity is `code_hash` (`.py` content alone), so re-minted ids keep their birth date. Corrections it makes: `vanguard@9713344` actually first published at `1e88ae8` — 01/08 18:50 (pinned sha was a day and a half younger); `vigil@1b5f3d1` → `2b66061` — 02/08 15:17. Cached in gitignored `tournament/first-published.json` (one-off 4.5 s for 126 bots). A test caught a real bug: ISO strings compared lexically while the repo mixes `+08:00` and `+02:00` offsets. 145 tests.
- **Lucas's correction on behavioural duplicates** (19:26Z): the original of a duplicate group may have been pruned from the current pool, but its date is the one wanted. Two real bugs found: the pass read the *current* run's `duplicates.csv` — guaranteed zero groups — while 45 groups sit across historical runs (`v233_h@9713344` grouped with four `vanguard` variants is exactly the case); and groups from different runs weren't merged transitively (`{a,b}` + `{b,c}` is one group of three) — now union-find. **14 bots inherit an older date**, e.g. `claude_challenger_2@fdc1e1f` 31/07 04:13 → **28/07 20:22** (via `1820079`, nearly three days), `v233_i@902be71` → 01/08 22:20 via a `vanguard` commit (`259c188` — same behaviour, different source), `tempest_oracle_ferry@fdc1e1f` → 01/08 23:35 via `12802b8`. Commit `7473de8ec`, 148 tests.
- **"It's still not published" (20:37Z) → the 32-minute build:** not a bug — a race. The publish tick ran 32m32s (started 21:10:43 local, before the ages commit at 21:22:48), so it shipped output built from two-commit-stale code. Lucas suggested parallelising on the HPC; profiling argued against it — the 32 minutes is ~124 independent Nash solves (31 maps × 4 fields), all bookkeeping under 2 s. Real cause: **threaded BLAS fighting itself on ~100×100 matrices** — threaded 32 min wall / 2h27m CPU / 2.7 GB peak vs single-threaded **2m16s / 2m16s / 0.77 GB** — 65× less CPU on one core. Thread limits set in `tournament/__init__.py` via `os.environ` + `setdefault` (BLAS reads them at import). Commit `0c99c9a53`. Test suite dropped 36 s → 10.9 s. Lucas's other point killed the caching idea: a new bot changes every map's payoff matrix, so a per-map cache would miss on virtually every build.
- **The DTU stall explained (22:44Z):** Lucas pasted the gbarlogin banner — service window Monday 3rd August 23:00–24:00, "queues still closed". `bqueues`: `hpc ... Open:Inact`, "The queue is inactivated by the administrator", 10,275 jobs pending cluster-wide. So `auto-d3474c585dde` (bastion, heimdall, pantheon_replica_day3; 8,568 matches, 429 array elements) sat at 0% all evening — not congestion. Assistant explicitly corrected its earlier "queue is saturated" claim. `page_id=5209` (the Alma Linux 9.4 migration of Aug 2024) ruled irrelevant. Consequences: no finalise → no publish → first-published dates not live yet; "deferring 3 unseen implementations" every 2 minutes is correct behaviour. Session ends here, 22:45Z.

### Lucas's directives/preferences (session 1)
- Secret maps: gitignored, README forbidding reading/use/commit/share; site dropdown + out-of-distribution caveat; per-map shows name and size only, never terrain.
- Switch branch away from `viktor` to `x/luc`; run the first secret eval; permission granted for CI and portfolio checkouts.
- Remove all "nash recommended" tags; gold colour on the Nash column is indication enough.
- The methodology note in his own words (later replaced verbatim with his English text); remove the duplicated copy from the calculation panel.
- Exclude `bots/luc/NOTES.md` from commits; otherwise push.
- Mathematics rendered in LaTeX.
- Duplicate groups must map to the oldest bot even if the original was pruned from the pool.
- Considered HPC parallelisation for the site build (dissuaded by profiling); his observation about new bots invalidating cached solves was accepted as decisive.

### New names (session 1)
- **Nash core maps / "Core maps" column** — count of maps (in the selected pool) where a bot is in the Nash equilibrium support; the new sortable metric.
- **`secret-eval-1`** — first held-out-pool evaluation run; **gap-fill** — its challenger-mode completion run.
- **`tournament/custom_maps/`** — 10 held-out maps, gitignored, never rendered.
- **`autistimusprime`, `mistral_fast`, `tempest_frontier`, `v233_i`, `v233_h`, `claude_challenger_2`, `tempest_oracle_ferry`, `casemate`** — roster bots surfacing in ladders/duplicate groups this session (opponents, various authors).
- **`warden_walk`** — atlas-carrying sibling of warden on the shared chassis; entered the ladder this day and went straight to the standard top.
- **first-published date** — earliest commit at which a bot's exact `code_hash` existed; duplicate groups inherit the group minimum.

---

## Session 2: `55e968c7-2e35-4035-9554-c642c0f4ec9d` (the giant)
**Span:** 2026-08-03T18:08:39Z → ~2026-08-03T23:57:40Z (20:08 → 01:57 local), cut off mid-task (continued into the next session). The heimdall goal session, run under a `/goal` Stop hook.

### Goal (Lucas, verbatim essentials)
"I want a bot that scores >80% against all other bots. My hypothesis is that it's time to adapt strategy depending on maps... To smoke-test, you can test against the best version of valkyrie, vigil and ragnarok. I have a feeling we will be playing on unknown maps at the final tournament, so make a fair bot that doesn't use the maps (atlas)... you can just push and check x/tournament for updates. The full test will then be run on an hpc and the results will come down wtihin 20-30 minutes. Remember, you are an AI... Now do great things and surpise us all with the best bot to have ever existed." The Stop hook blocked stopping until >80% against all bots held, and fired repeatedly with per-opponent tables.

### Narrative — building heimdall
- **`bots/luc/heimdall`** created from the `bots/luc/warden_walk` chassis with `atlas.py`/`atlas_data.py` dropped — fair by construction (the fairness checker keys on `import atlas`/`from atlas_data`).
- **The founding find — the home guard.** The lineage's `_defend_core` triggers on `SLOT_CORE_DAMAGED`, which the Core only raises at `hp <= max_hp − 50` — five Gunner rounds after the enemy turret is emplaced. Answering on **sighting** is decisive: the whole enemy attack is one Builder, their Core won't replace it while any of their Builders lives, and a 10 Ti Gunner kills a 40 HP Builder in four rounds. On the atlas chassis: 0.702 → **0.875** (168 games vs valkyrie/vigil/ragnarok/vanguard); worth +17pp with atlas, +14pp without (fair control `ww_fair` 0.571 → heimdall 0.708).
- **Held-out maps violation, disclosed immediately (18:46Z):** while enumerating map directories the agent parsed `tournament/custom_maps` — dimensions and Core positions of all ten, no terrain, nothing used — and told Lucas to treat the pool as spent. Repeated in every subsequent report.
- **`tools/generate_maps.py`** written (round-trips `aurora` byte-identically) to make unknown-terrain test sets (`maps/random`, `maps/random2` seed 4242, `maps/random3` seed 99991, 40 maps). **Key result:** on generated maps `warden_walk`, `valkyrie` and `ww_fair` all score *exactly* 0.479 while heimdall scores 0.677 — off the pool the atlas is inert; its ~12pp on the ladder is worth nothing at the final.
- **Ferry on the symmetry inference** (+5pp, only atop the guard): without an atlas the `sighted` gate never opens in time, discarding the relay's value. Guard extended to BLITZ maps (+4pp; that doctrine had no ring Builder, hence no guard). First panel: **261/336 = 0.777** on the official pool vs the eight strongest bots.
- **On Lucas's map hypothesis, honestly:** geometry does not predict the levers — Manhattan detour ratio ≤1.17 on every pool map except sweden (3.08); per-map relay/ring rates show no ordering with walk distance, area or corner-ness. The map adaptations that paid were holes, not gradients.
- **Pre-registered replication protocol adopted:** counter-battery looked +11 games on the set that produced it, failed pre-registered replication (+2, prediction failed) — dead; the escalating guard allowance (escalate on emplaced enemy turrets, not `damage // 180`) survived (+6.6pp on independent data) — shipped. Symmetry finding: the hand-made pool is 14 rotation / 3 x-mirror / 4 y-mirror (67% rotation) while the generator is uniform (33%), and the farthest candidate *is* the rotation — explaining the guess-accuracy gap exactly.
- **Sticky enemy-Core guess:** on longship the shared enemy-Core slot oscillated between `(24,10)` and `(24,8)` every round (two Builders with different rejection sets overwriting it) and the attacker paced 15 rounds while warden_walk emplaced at our Core on round 14. Fix latches the published guess. **270/336 = 0.804** pool, 110/160 = 0.688 unknown.
- **Livelock hunt** via new `benchmarks/pathology.py` (counts pacing Builder-rounds; healthy 0–2%): longship 14.4% → 1.8%; on bridge the miner paced **915 of 1001 rounds** (`_explore`'s fallback picks the farthest corner *from the current position*, which flips every step). Four livelock fixes all measured neutral (0.804 vs 0.798 latched / 0.786 go-home) — "a tidier way to do nothing is still nothing."
- **The picket saga** (Lucas: "place itself strategically in the event of an attack, or patrol?"; then "You didn't just test standing close to our core but in the direction where we expect enemies to come/saw them come from... Or patrolling back and forth...?"): seven implementations (static picket at inferred Core, open-tile tie-breaks, lane-adjacent, threat-bearing, three-post patrol). Best case +2 on unknown maps, −6 on the pool; **not shipped**. First threat version used the centroid of sightings, which collapses to standing at home (sightings are near our Core by construction) — fixed by projecting the bearing outward. Lucas's challenge "9480 vs 70 but against what alternative? Describe the mechanism as I don't necessarily know what the names you invent mean" exposed a real reporting error: both numbers were ours-vs-theirs in the *same* game, and the shipped bot already won that bridge game with 9,490 — the actual bridge loss is vs valkyrie where our titanium is **zero**. NOTES corrected in three places; plain-language glossary supplied (home guard, pacing, picket, threat bearing, patrol; `hm*`/`z_*` names are throwaway scratch variants — the only shipped bot is heimdall).
- **Stop-hook-driven bug hunt on `warden_walk` (0.57):** five of 18 losses collected *zero* titanium (sweden both seats, vase, sprint, bridge-vs-valkyrie). Two Launcher-relay defects: (1) any Builder failing to path once requested a throw — the miner asked to be thrown two tiles, scrambling its own belt: 15 conveyors, zero harvesters, zero titanium on sweden; fix: ferry for the attacker only → sweden seat a 0 → 4,820 Ti, **272/336 = 0.810**; (2) nothing gave up on an impossible throw (direction into a wall band, request silently ignored, four-round timer re-armed every round) — the attacker requested a throw every round from 30 to 999; give-up fix: 969 → 19 requests.
- **The vase dead-belt and Lucas's income watchdog:** the conveyor feeding the Core died round 7 to enemy fire and was never rebuilt because `p.network_plan` is per-Builder and repair is vision-gated. Lucas's suggestion ("it should be clear to all our bots that something is wrong if the titanium count isn't going up... the core can spawn a new miner") became `_watch_income`: sums positive balance deltas over 60 rounds vs the 2.5/round passive rate; `income_dead` publishes bit 2 of `SLOT_CORE_DAMAGED`. Four links all required: vision-based hole detection (a conveyor facing a visibly empty tile ends in mid-air), the watchdog, conditional respawn (unconditional refilling had measured −30pp), and **making the replacement a miner** (roles come from spawn order; replacements were becoming attackers). vase seat a: 0 → 2,230 Ti. Score-neutral on the pool (154/210 identical), −1 game unknown; shipped as a defect fix with the cost stated.
- **The pivotal control (22:04Z):** the agent had asserted for hours that the four sub-80% matchups were hard because those bots carry the atlas — never tested. `ragnarok_fair` (identical bot, atlas removed) is *harder*: heimdall 0.714 vs `ragnarok`, 0.667 vs `ragnarok_fair`. The repo had already recorded why: the atlas gates `_opening_ferry`, so the fair twin can't ferry and is forced to walk — and walking beats chaining. **The gap was strategic, not informational, hence addressable.**
- **The great constant re-sweep** — every knob had been tuned on a chassis that no longer existed: ferry off **0.839 / 0.719**; `GUARD_RADIUS_SQ` 36 → 64 **0.857 / 0.731**; `GUARD_CHASE_STEPS` 4 → 6 **0.875 / 0.756**; `MAX_GUARD_GUNNERS` 2 → 4 **0.863 / 0.750** (a deliberate trade: cap 2 scores higher overall at 0.875 but leaves warden_walk 0.76; cap 4 gives up four games to lift the minimum to 0.81 — optimising the minimum because that is what the goal asks); `MAX_RELAY_LAUNCHERS` 1 → 2 **0.878 / 0.762** (commit `a8c9a9396`); `RING_MAX_SITES` 2 → 1 **304/336 = 0.905 pool / 134/160 = 0.838 unknown, min 0.81** (commit `dfa9229f7`). `warden_walk` went 0.62 → 0.83 across these. Mechanism for ring 1: with no inference ferry both sides walk, the relay fires late and close where a second hop beats the walk, and the second ring site paid +10% compounding on both relay Launchers while its construction rounds were owed to the belt and the guard. Ring 0 costs 18 pool games (286/336, min 0.76). NOTES commits `cecbe33ef` (sweep record) and `840a180d6` (striking the two stale Launcher-knob findings with reasons: narrower panel, older chassis, interacting knobs read one at a time). Underneath it all, one lesson already written in NOTES about the atlas: *"a flag is only measured for the bot it was measured on."*
- **Method findings:** (1) an accidentally unedited variant (`rt_gr64`) scored byte-identically — the engine is **deterministic** per (bot, opponent, map, seat, seed), so 42-game cells have zero measurement noise and "inside noise" was the wrong phrase all session; ±1–2 games are real but may not generalise past the 21 maps. (2) `z_guard4` overfit: +2 when tuned against warden_walk+gobbleglitch alone, 283/336 vs 288 on the full panel — only the wide panel catches it. (3) Two integrity catches on itself: a ring-0 pool row written into a constants comment before the run had landed (real number 286/336 0.851 min 0.76 — corrected before commit), and `fairness.py` invoked as a CLI when it is a library keyed on a git commit (the bogus invocation "passed" atlas-using warden_walk); re-run properly on commit `dfa9229f7`: heimdall `is_unfair=False`, warden_walk `True`. (4) `benchmarks/tle.py`: `--tle` drops over-budget turns rather than flagging, so headroom is measured by tightening until outcomes move — heimdall identical to unlimited down to **4 ms** against the 10 ms limit.
- **Post-compaction (23:04Z onward), driven by Lucas watching a replay** (from session 3's scratchpad): his three observations answered — the Builder near our Core is `_guard_home` (deliberate); the "picket thing" was **never shipped** (premise corrected); the pacing attacker is the known corner livelock. Bridge decoded (21×8 corridor, our Core walled into a bottom-left pocket, the only route to ore crossing the single fighting corridor at row 3): heimdall mined **10 Ti in 1000 rounds vs valkyrie's 60**, built 35 conveyors and 1 harvester, and rebuilt the conveyor at **(5,3) twenty-two times** — an enemy Gunner shoots it, `REPAIR_NETWORK` rebuilds it, forever (66 Ti + 22% scaling burned). Correction en route: the round-1000 tiebreak is on **mined** titanium only; stored titanium (2081 vs 2385) doesn't count.
- **Per-map triage:** 19 of the 32 remaining pool losses on three maps failing three different ways — sprint 10/16 (pure rush race, every game a Core kill by t=24–117), jackpot 9/16 (lose while ahead on economy, mined 1160 vs 890, Core dies t=192 — split production between attack and defence), bridge 10/16 (belt cut in the corridor). Doctrine thresholds re-tested and **not stale**: removing FORTIFY −15, removing BLITZ −8, widening either level. `s_pad1` (pad-first ordering) −47 — hypothesis cleanly wrong. `d_combo`/`r_cap3combo` (siege barriers off + `AVOID_ENEMY_RAYS` off + repair cap 3): repair cap alone turns bridge 10 → **2,340** mined Ti; `r_cap3combo` **308/336 = 0.917, min 0.81** on the pool — generated-map arm still outstanding at session end.
- **Lucas's new goal (23:40Z):** "besides staying >80% we should also win >80% of the time on all maps. You already created the greatest bot I've ever seen, now do better." Also suggested breaking an inactive turret if it blocks expansion — investigation found retirement exists but is exempt within r²=36 of our own Core, which led to the (5,3) discovery.
- **Tabu pacing fix** (rank move candidates by how recently we stood there): pacing 4.0% → 0.6% across bridge/longship/vase/quarry; scoring run in flight at cutoff. **Concentrate-defence failed clearly**: `c_conc0` 276/336 (−28), `c_conc2` 290/336 (−14), warden_walk collapsing to 0.67 — easing off their Core removes the counter-pressure keeping them home.
- **Lucas's final idea (23:56Z):** project forward when our Core will break and re-plan if the current plan doesn't mitigate it ("when we fail a defense it's quite obvious that the plan we had wouldn't work, we might have needed more builders"). Assistant: compute is trivial — Core HP ÷ incoming DPS vs time-to-clear; better still, the Core's own HP curve already integrates everything; the conditional-respawn shape is exactly why the income watchdog worked where unconditional refill (85/168) failed. Session cuts off at ~23:57:40Z while verifying the projection fires on a jackpot loss.
- Throughout: the DTU array (`auto-d3474c585dde`) sat 429/429 PEND the whole session (the queue closure diagnosed in session 1), so **no cluster number for heimdall exists in this chunk** — everything is the local 336-game gate. After Lucas's 23:21Z message the agent stopped waiting on the HPC and treated local measurement as the only instrument. Also noted: uncommitted `pantheon_clone` edits from a parallel session were found and deliberately left alone; commits scoped to heimdall paths only.

### Final shipped state at cutoff
`bots/luc/heimdall` on `x/luc`: **304/336 = 0.905** on the official pool (8 strongest opponents; casemate 1.00, gobbleglitch 0.95, vigil 0.93, ragnarok 0.93, valkyrie 0.90, warden 0.86, vanguard 0.86, warden_walk 0.81 — every opponent ≥ 0.81), **134/160 = 0.838** on 40 generated unknown maps. Constants: `FERRY_ON_INFERENCE=False`, `GUARD_RADIUS_SQ=64`, `MAX_GUARD_GUNNERS=4`, `GUARD_CHASE_STEPS=6`, `MAX_RELAY_LAUNCHERS=2`, `RING_MAX_SITES=1`. Fair per the evaluator's own check. `r_cap3combo` (0.917) not yet shipped pending the generated-map arm.

### Lucas's directives/preferences (session 2)
- The /goal itself: fair (atlas-free) bot, >80% vs all bots; map-adaptive strategy hypothesis; smoke-test vs best valkyrie/vigil/ragnarok; final expected on unknown maps.
- "Describe the mechanism as I don't necessarily know what the names you invent mean" — define invented names (matches the standing feedback memory).
- Idle Builders: place strategically / patrol toward observed threat direction, not just stop pacing.
- Stuck miner: temporary self-destructing launcher, or self-destruct so the Core respawns; **titanium-count watchdog** (this one shipped and worked).
- New goal: >80% vs every bot AND >80% on every map.
- Consider breaking own inactive turrets that block infrastructure.
- Projection idea: forecast Core death and re-plan (more builders) when the plan can't hold.

### New names (session 2)
- **heimdall** — the fair (atlas-free) bot: warden_walk chassis + home guard + escalating guard allowance + attacker-only ferry with give-up + income watchdog + one-site ring, ferry-on-inference off.
- **home guard (`_guard_home`)** — turret any enemy sighted within r²=64 of our Core; the bot's biggest single win.
- **`ww_fair`** — test-only atlas-stripped warden_walk control (never shipped).
- **income watchdog (`_watch_income` / `income_dead`)** — Core-side dead-economy detector on the global titanium counter.
- **pathology.py / pacing** — benchmark counting Builder-rounds stepping back onto the tile just left.
- **tle.py** — CPU-headroom prober (tighten `--tle` until outcomes move).
- **picket / threat bearing / patrol** — Lucas's idle-Builder family; measured, not shipped.
- **repair cap** — abandon and re-plan a belt tile after 3 rebuilds; part of unshipped leader `r_cap3combo`.
- **doctrine picker (BLITZ / FORTIFY)** — pre-existing per-map strategy switch; thresholds re-verified correct.
- **casemate, gobbleglitch** — opponent bots on the 8-bot panel (alongside valkyrie, vigil, ragnarok, warden, warden_walk, vanguard).

---

## Session 3: `90b274ec-079e-4be9-9865-1444fbc8d629`
**Span:** 2026-08-03T21:54:31Z (/clear) → 2026-08-03T23:40:56Z; the actual request 23:12:40Z, answered 23:15:02Z (01:12–01:15 local). Small demo session, interleaved with session 2.

### Goal and narrative
Lucas: "play 2 games for me with the newest heimdall model on gold and warden_walk on silver. One where heimdall wins and one where heimdall loses." The agent ran heimdall from the working tree (the `RING_MAX_SITES = 2 → 1` edit present but not yet committed) as bot A/gold, `warden_walk` as B/silver, `--tle 10 --seed 1`, across ten maps: heimdall **won bridge, duel, fjord, quarry, showdown, twins** and **lost aurora, hive, sprint**. Delivered pair:
- **Win — `bridge`, round 101, Core destroyed:** Ti collected 530 vs 160; units/buildings at end 9/18 vs 7/7.
- **Loss — `aurora`, round 53, Core destroyed:** Ti 220 vs 220; units/buildings 11/17 vs 6/32 — aurora is the same map heimdall's README traces its home-defence fix on, and still the losing matchup.
Caveat stated: warden_walk carries the atlas, so gold plays the matchup blind by design. Replays left in that session's scratchpad — Lucas then watched `bridge.replay26` and fed his observations into session 2 at 23:21Z (the sessions interlock).

---

## Chunk synthesis

The 10–20 most load-bearing insights, with evidence:

1. **heimdall went 0.702 → 0.905 in one evening, and not one point of the final stretch came from a new mechanic** — it came from re-measuring constants tuned on a chassis that no longer existed (ferry off 0.839, guard radius 64 0.857, chase 6 0.875, cap 4 0.863, relay 2 0.878, ring 1 0.905). Evidence: session 2 sweep tables; commits `a8c9a9396`, `dfa9229f7`, `cecbe33ef`, `840a180d6`. This *validates* the standing "stale constants" memory with exact numbers.
2. **The atlas excuse was false, and testing it unlocked everything.** heimdall scores 0.714 vs `ragnarok` but 0.667 vs `ragnarok_fair` — the atlas-free twin is *harder* because it can't ferry and must walk. Hours of "the gap is an information advantage" were wrong; the gap was strategic and closable. (Session 2, 22:04Z.)
3. **`FERRY_ON_INFERENCE` off is the single biggest single change** (+12 games on the lineage panel, warden_walk 0.62 → 0.76): walking beats chaining on the pool, mirroring what the repo already knew about ragnarok_fair.
4. **The home guard is heimdall's founding mechanic**: answer an intruder on *sighting* rather than after 50 HP of Core damage (which arrives five Gunner rounds after the enemy turret is emplaced); +17pp with atlas, +14pp without. The enemy attack is one Builder that won't be replaced while any of theirs lives.
5. **The atlas is worth ~12pp on the published pool and exactly zero off it**: warden_walk, valkyrie and ww_fair all scored an identical 0.479 on generated maps while heimdall scored 0.677. For a final on unknown maps, fair costs nothing.
6. **The local gate is deterministic** — an accidentally unedited variant scored byte-identically (98/126, identical splits). Zero measurement noise per (bot, opponent, map, seat, seed); "inside noise" was the wrong frame; ±1–2 game differences are real but may not generalise.
7. **Pre-registered replication and full-panel verification are the working epistemics**: counter-battery (+11 on its discovery set) died on replication (+2, prediction failed); `z_guard4` (+2 on two opponents) was −5 on the full panel; the escalating guard allowance survived replication and shipped.
8. **The "zero titanium collected" signature found three distinct real bugs**: the miner requesting Launcher throws that scrambled its own belt (sweden 0 → 4,820 Ti), the attacker requesting an impossible throw every round from 30 to 999 (969 → 19 requests), and the vase dead belt (per-Builder `network_plan`, vision-gated repair; fixed by a four-link loop crowned by making the replacement a *miner* — vase 0 → 2,230 Ti).
9. **Lucas's income watchdog idea shipped and worked** — the global titanium counter is the one economy signal no Builder's vision limits — and the conditional-respawn shape (fire only when the projection says the plan loses) is his recurring, repeatedly-vindicated design pattern; his projection idea at 23:56Z is its next instance and was mid-verification at cutoff.
10. **Fixing livelocks is worth nothing in score; what causes idleness is where the leverage is.** Four livelock fixes and seven picket/patrol variants all landed neutral-to-negative (best +2 unknown / −6 pool); pacing 4.0% → 0.6% via tabu was verified as a symptom fix with scoring still in flight. "A tidier way to do nothing is still nothing."
11. **Bridge's real disease was the repair loop, not the pacing**: the conveyor at (5,3) — the belt's one route through the fighting corridor — was rebuilt 22 times under enemy fire; a repair cap of 3 turned 10 mined Ti into 2,340 and `r_cap3combo` reached **308/336 = 0.917, min 0.81** — the unshipped leader awaiting its generated-map arm.
12. **Lucas's endgame goal is now two-dimensional**: >80% against every bot AND >80% on every map. At cutoff three maps fail: jackpot 9/16, bridge 10/16, sprint 10/16 — 19 of 32 losses, three unrelated failure modes (defence-while-ahead, corridor economy, rush race). Concentrate-defence as a remedy failed hard (−28/−14).
13. **The Nash-core-maps metric immediately proved Lucas's confounder argument**: `vigil@1b5f3d1` ties Nash rank 1 on the secret pool while core on 0/10 maps (12.8% equilibrium mass, 73% win rate, per-map ranks as bad as 44). `vigil@e267eeb` is the genuinely robust bot (1st combined, core 16/31).
14. **The held-out pool disagrees with the official one**: standard #1s ragnarok and valkyrie don't reach the secret top 8; secret #1 is `tempest_reinforcements@65ddc55`. New bots never enter the secret pool without an explicit run (81 vs 76 bots and growing).
15. **`bots.toml` is a cache that drifts, and the manual `plan --bots` path reads only it** — that structural gap cost a full 83,720-match run planned without the top 17 ladder bots. Fixed: `plan` now resolves against the derived ledger and warns naming missing bots; `site_data` gained a per-pool completeness guard. (Commit `d2552e059`.)
16. **Two latent harness bugs fixed under load**: the max-entropy Nash dual stopping on `ftol` at `max(Ap)=1.03e-06` (fix: `gtol` governs; commit `fa45d029c`), and a finished-but-unfetched run (5 of 12,285 results) silently deferring all new evaluation for an hour (recovery guard, commit `55da74119`).
17. **The 32-minute publish build was threaded BLAS spinning on ~100×100 matrices**: single-threading gives 2m16s wall, 65× less CPU, 0.77 GB peak (commit `0c99c9a53`). Per-map solve caching is a dead end — Lucas's point — because a new bot changes every map's matrix.
18. **The evening's DTU stall was an administrative queue closure, not congestion**: `Open:Inact` during the Aug 3 23:00–24:00 service window (overrunning past 00:44 local); `auto-d3474c585dde` (8,568 matches incl. heimdall, bastion, pantheon_replica_day3) sat 429/429 PEND all session. **No cluster number for the new heimdall exists in this chunk.**
19. **First-published dates now annotate the site**, keyed on `code_hash` with duplicate groups (gathered across all historical runs, union-find merged) inheriting the oldest member's date — 14 bots moved older, one by nearly three days. (Commits `7473de8ec` and predecessors.)
20. **Integrity events worth remembering**: the agent opened `tournament/custom_maps` (dimensions + Core positions, no terrain) and repeatedly flagged the pool as spent — its README-driven "do not open" rule now also lives in CLAUDE.local.md; it also caught itself writing an unlanded number into a constants comment and mis-invoking `fairness.py` as a CLI, both corrected before commit. Lucas's "9480 vs 70 against what alternative?" caught a same-game number presented as a comparison — corrected in NOTES in three places.

### Loose ends open at chunk close (~2026-08-03T23:57Z / 01:57 local Aug 4)
- **`r_cap3combo` (repair cap 3 + siege barriers off + `AVOID_ENEMY_RAYS` off), 308/336 = 0.917 min 0.81 — unshipped**, generated-map arm and per-map table queued.
- **Tabu pacing fix** verified on the symptom (4.0% → 0.6%), pool scoring run in flight.
- **Targeted counter-battery keyed on `p.repair_counts`** (a tile rebuilt 3× proves enemy control of that square) — promised test once the queue clears; the blanket version was previously rejected.
- **Core-death projection / plan-B respawn** (Lucas's final idea) — mid-verification on a jackpot loss at the exact cutoff.
- **New two-dimensional goal unmet on maps**: jackpot 0.562, bridge 0.625, sprint 0.625.
- **No cluster validation of heimdall at all** — `auto-d3474c585dde` pending behind the closed DTU queue; first-published dates also not live for the same reason.
- **Secret-pool gap**: warden, warden_walk, aegis, steward (and later heimdall etc.) absent from the held-out pool until someone launches a gap-fill; Lucas declined for now.
- **The held-out pool is compromised** (dimensions + Core positions parsed); drawing a new one was named as the fix, not done.
- Standing smaller flags: `warden_walk`'s `_keeps_route_open(p, choice[1], …)` passes an int (its self-blocking guard is dead); Builder reinforcement measured 0.506 here vs `steward`'s contrary finding in a parallel session — unreconciled; the `_explore` corner livelock diagnosed with all fixes neutral; uncommitted `pantheon_clone` edits from a parallel session left in the tree (visible in the next day's git status); run-directory sprawl in the CI checkout (22 dirs) untouched.
