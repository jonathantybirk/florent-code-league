# Digest H — Claude Code sessions of 2026-08-02 (daytime and evening)

> **Measured before the Aug 4 turret patch (fcode ≤ 2.3.3).** Everything below was
> measured when turrets were stronger. The 2026-08-04 balance pass (fcode 2.3.4)
> changed the Gunner to 25 HP (was 40), 20 Ti (was 10), +20% cost scaling (was
> +10%), 7 damage (was 10) and 4 ammo per shot (was 2), and the Sentinel to 40 HP
> (was 30) on a 2-round reload (was 3). That balance pass is the only rules change
> in 2.3.4, so conclusions here that do not turn on turret strength still stand —
> but every turret-heavy number needs re-measuring before it is trusted again.

All timestamps are UTC (transcript `Z` times). Note: log lines quoted inside sessions (e.g. "failing since 15:31", "deployed at 16:37:25") are Europe/Copenhagen local = UTC+2; where both appear I say which is which. Five JSONL files, but effectively three working threads: one standalone morning session; one afternoon thread (5d7e1ef9 → after `/clear` + session-limit gap → 0f3c24e1); one parallel thread (39ebc3bb → after `/clear` → 797291d9).

---

## Session 1 — 152ba9fb-b113-4cc7-a80f-8a2fc1d883ee (10:04:49–10:43:41 UTC)

**Goal:** Diagnose a suspected regression in `tempest_reinforcements` between commits `edf93ad` → `51ad530` ("Preserve build budgets and turret firing lanes"); Lucas suspected the build-budget change.

**Narrative.** Claude identified three bundled changes in `51ad530`: (1) titanium reserve for ammo conversion changed from a fixed 60-Ti floor to `max(MIN_TITANIUM_RESERVE, harvester_cost, launcher_cost)` — dangerous because build costs scale additively (+20% per builder, +5% per harvester, +10% per turret) and never decay, so the reserve grows unboundedly against `AMMO_TARGET = 120`; (2) a new `_preserves_friendly_turret_lanes()` gate on four gunner-placement sites; (3) ~10 new `PLAN_FAILED` print() calls. Lucas directed: "Ignore the 10ms limit that is a completely separate test that the 025 commit fixes anyway" (referring to `025dd519f` "Eliminate repeated turret planning searches"), then: "Test out the hypotheses, test against autistimusprime@ecd4d24 and undertow@da3fd8a to see if we can improve."

Sweep over all 21 maps × both sides × both opponents (84 distinct cells; engine seeds proved completely inert — 3 seeds gave exactly 3× the seed-1 result):

| variant | vs undertow | vs autistimusprime | overall | flips vs base |
|---|---|---|---|---|
| base (HEAD) | 50.0% | 54.8% | 52.4% | — |
| noreserve | 50.0% | 54.8% | 52.4% | +0 −0 |
| nolanes | 52.4% | 54.8% | 53.6% | +1 −0 (p=1.0) |
| pre_regression (edf93ad) | 52.4% | 54.8% | 53.6% | +1 −0 (p=1.0) |

**Both hypotheses refuted as regression causes.** The reserve change altered zero of 84 outcomes even though instrumentation showed it genuinely binds (reserve climbs to 74–105 Ti, changing conversion in 76–109 rounds on vault/sweden). The lane gate was worth exactly one map cell (vault, playing second) — and that single cell is the *entire* edf93ad→51ad530 delta. The regression Lucas saw did not reproduce against these two opponents.

**The real finding:** ammo sits at 0–1 for essentially the whole match in both versions — and `MIN_AMMO_FOR_GUNNER = 20` gates all four gunner-construction paths, so the bot was near-permanently unable to build or fire turrets. Fix: a `COMBAT_AMMO_FLOOR` in `core.py` that outranks the construction reserve — below the floor the Core converts titanium→ammo down to a 10 Ti emergency reserve. Result: **52.4% → 78.6% overall** (undertow 50.0%→83.3%, autistimusprime 54.8%→73.8%), +22 wins −0 losses paired, p < 0.00001. Floor sweep 20/40/60/**80**/100/120 → 70.2/75.0/76.2/**78.6**/75.0/70.2%; 70–90 is a plateau (+20 or better, zero losses), so 80 is not overfit. Test suite 82 passed; rewrote `test_ammo_conversion_preserves_scaled_construction_cost` into two tests; `test_known_map_strategy.py::test_bot_1_vs_starter_on_twins` fails identically on unmodified HEAD (pre-existing, unrelated).

**Decisions:** Lucas: "Push the changes" → pushed as `65ddc5545` to `origin/x/luc` (core.py + tests only). The `51ad530` reserve and lane-gate changes were deliberately left in place — no measurable harm.

**Names:** `autistimusprime@ecd4d24`, `undertow@da3fd8a` — pinned opponent bots used as the test panel. `COMBAT_AMMO_FLOOR` — new tempest_reinforcements constant (value 80) making ammo conversion outrank the construction reserve.

---

## Session 2 — 39ebc3bb-b470-4ad1-918d-7c1727ccddd7 (12:54:03–13:27:16 UTC)

**Goal:** A templated inspection prompt (later revealed to be generated by a button on Lucas's portfolio website): open 10 random rating matches between `vigil@18b749d` and `tempest_reinforcements@38f9c23` from `tournament/runs/auto-012bed9301ac/matches-distinct.csv`, rerunning each with `uv run fcode run ... --seed 1 --tle 0` and opening the visualizer.

**Narrative.** The run directory lives in the sibling checkout `/home/Ucals/projects/florent-code-league-ci`, not the llm-rl repo. Both bots were already staged as pinned trees. `--watch` blocks in `serve_forever()`, so Claude used `--replay <file>.replay26` + backgrounded `fcode watch` per match. All 10 replays reproduced the CSV exactly (same winner, same turn count). Sample head-to-head: **vigil 6 – 4 tempest_reinforcements**. Notable rows: bridge went 1000 turns to a titanium tiebreak (vigil); twins ended turn 31, sprint 38, duel 34. Replays saved to `/home/Ucals/projects/florent-code-league-ci/replays-vigil-vs-tr/`; 10 visualizer servers left running (plus one stale from a previous session).

The prompt's trailing request about "the prompt on the live website" was ambiguous; Lucas clarified: "It's my portfolio site, it's in my ~projects/portfolio." Claude found `buildInspectionPrompt` in `portfolio/src/components/BotRankings.tsx:296` — the exact generator of the session's own opening prompt — and fixed the two things that had misled it: (1) `--watch` was the headline instruction but hangs for N>1 matches, so `--replay` + `fcode watch` became the documented path with an explicit warning; (2) "the repository root" was ambiguous across the two checkouts — now says to pick the checkout containing the run directory. Lucas: "yes commit and push" → commit `895dcec` to portfolio `main`, Workers Builds check-run completed/success, new text confirmed in the built bundle. Caveat: curl of guessed hostname `portfolio.lucas-lista.workers.dev` failed (custom domain `lucasrgpedersen.com` not yet attached to the Worker), so live-JS verification was inconclusive.

**Names:** `buildInspectionPrompt` — portfolio-site function generating replay-inspection prompts for Claude sessions. `auto-012bed9301ac` — the tournament run sampled.

---

## Session 3 — 5d7e1ef9-25f2-4760-89c1-4bb1c30741cd (12:19:22–15:01:06 UTC; queue ops to 15:56)

**Goal (evolving):** map-strategy analysis of tournament data → when to branch strategies by map → build a fair multi-strategy bot → then firefighting the live ladder and fixing the evaluator.

### 3a. Map-strategy clustering (12:19–12:49)
Analyzed all **138,785 rating matches (91 bots × 21 maps, both seat orders)** from `tournament/runs/*/results/`. Per-map Bradley-Terry residuals (bot global strength divided out), maps clustered by residual correlation. Split-half validation: identical 4-cluster partition (adjusted Rand = 1.00), per-map residual vectors correlate 0.68–0.98 across halves. Four groups:

- **A. Cramped & titanium-rich** (string, jackpot, vase) — army+fortification wins; pure economy punished (−0.57 logit/sd).
- **B. Walled lane** (bridge, quarry, sweden, crossfire) — fortify + eco-brawl; rush punished hardest (−0.21 avg, bridge −0.88).
- **C. Short-fuse open** (aurora, pinch, showdown, sprint, strait, twins) — rush strongest (+0.28/sd).
- **D. Big open** (atoll, duel, fjord, hive, longship, runestone, skerry, vault) — rush (+0.11); fortification hurts (−0.20).

Head-to-head, tempest/mistral family (rush; ~5.6 buildings, kills median turn 38–40) vs undertow/vanguard family (eco-brawl; ~20 buildings, 900+ titanium): rush wins **27.9%** on A (n=551), **24.7%** on B (n=736), **81.9%** on C (n=1104), **79.4%** on D (n=1472). Per-map extremes: **bridge 3.6% → showdown 96.9%** — same two bots, 93-point swing from terrain alone. Geometry predictors: wall fraction r=−0.65, openness +0.52, narrowest mid-map corridor +0.43, titanium density −0.43. crossfire is the neutral map (R²=0.02, residual sd 0.46, lowest of 21); cluster B is carried by bridge (residual sd 1.76, 3× field median). Seat advantage is map-dependent: atoll +0.43 logit for player A, strait +0.52 for player B.

### 3b. "When should we branch?" (12:36–12:49)
Lucas asked, in his own words: general algorithms vs map-specific strategies — "when is the right time to branch out? … it is also a bit of a fool's errand to try and make one algorithm that is best on all maps." Half-split out-of-sample analysis: 1 branch 63.7% WR, **2 branches 71.2% (+7.5pp)**, 3 → 71.1%, 4 → 71.6%, 6 → 71.7%, per-map oracle 75.2% (+11.5pp, 13 distinct bots, 6.3pp overfit gap). **The first branch captures two-thirds of everything map-awareness can buy; branches 3–6 buy ~0.5pp combined.** k=2 picked `vanguard_oracle` (closed) + `tempest_oracle_guarded` (open) unprompted. Reframe: branch on a *measured map property*, not map name. Turn-0 classifier: vertex min-cut between cores ≤ 2 → closed behavior (19/21 correct), adding `OR ti_total ≥ 19` catches quarry → 20/21; sole miss is pinch (has a choke but cores close enough to rush).

**Corrections Claude had to issue after Lucas pressed on "Name-keyed strategy is disqualified by the rules":** (1) Nothing in the published rules prohibits map-identity lookup — "drawn at random from the current competition map pool… announced at the start of the competition" (`game-rules-how-matches-work.txt:11`); the atlas exclusion is **Lucas's team's own fairness convention**, not a league rule (`atlas.py` docstring: "Published-map oracle deliberately excluded from fair bots"). (2) `autistimusprime`, `BLD4`, the `e_*` family and `tempest_reinforcements` also bundle an atlas — the name-based "oracle" filter had contaminated the numbers. Re-run on 68 atlas-free candidates / 36-bot field: 1 branch 60.7%, **2 branches 67.5% (+6.8pp)**, 4 → 66.9% (worse than 2), per-map 71.0%. Caveat: the +6.8pp requires forcing the geometric closed/open split; letting the dendrogram pick its own 2-way cut gives only +1.7pp. (3) Turn-0 min-cut isn't computable by an atlas-free bot (vision: core r²=36, builder r²=20; no full-terrain getter) — "turn-0 information is enough to *identify* the map on a known pool, but not enough to *measure* its geometry. That gap is the entire reason the atlas exists."

### 3c. prospect (12:52–13:18)
Built `bots/luc/prospect` — fair bot forked from vigil at `18b749dc7` with the atlas removed and a doctrine layer added (`doctrine.py`; Core classifies once on round 0 and publishes the verdict in spare high bits of `SLOT_OWN_CORE` — `pack_pos` tops out at 958 so bits 10+ were free; Builders read it back). Atlas removal also unlocked `_opening_ferry` for fair play. Of three turn-0 rules tested vs a rush-pinned twin: **Cornered Core (bridge, jackpot, string, sweden, vase): 8-2, shipped**; wall-density (adds crossfire): 0-2, cut; large+ore-rich (adds quarry): 0-2, cut. Doctrine decomposition: role reallocation to 2 economy / 0 attackers lost **2-12** ("removing the attacker is catastrophic"); turrets-only (field gunners) went 8-6. Against a genuinely strong third party (fair `vanguard` from `x/jon`): prospect 6/10 on fortify maps, 9/10 on rush maps — **identical to its feature-off twin** `prospect_rushonly`; the 8-2 mirror result did not generalize. On the 16 rush maps both variants produce byte-identical games (verified no-op). Lucas, 13:15: **"It feels like you are speaking in code honestly. I have no clue what you are trying to tell me… What do you mean A/B twin you never introduced me to two bots"** → Claude restated plainly: the feature doesn't help against a real opponent (6/10 either way); prospect is competitive (15/20 vs vanguard) but that's inherited from vigil. Lucas then diagnosed the design flaw himself: "did you get inspired by which bots won which maps previously? Or did you try to create map differentiation from the bottom up? Cause I would think it's vastly different strategies that won different maps." Claude conceded: analysis was top-down, build was bottom-up knob-turning; tempest ~5.6 buildings / ~90 Ti vs vanguard 15-19 buildings / 500-650 Ti vs undertow ~20 / ~950 — "different machines," and 2 field gunners moves 5.6 → ~7.6 when vanguard is at 19. Committed and pushed prospect + prospect_rushonly as `1b5f3d1b8`.

### 3d. janus (13:24–13:41)
Lucas: "Yeah man test it out, just check that it won't confuse the CI." Built `bots/luc/janus`: a round-0 dispatcher bundling the prospect (tempest) lineage and a vendored vanguard, choosing by the cornered-Core rule. Findings along the way:
- **Units do not share module globals** — each unit gets its own namespace (probed directly); the 16-slot store is the only cross-unit channel. Rules out Core-decides-once.
- **Discovery hazard:** `tournament/discover.py` treats any directory containing `main.py` as a bot; nested `janus/vanguard/main.py` would have registered a second `vanguard` and made `discover()` raise `ValueError: ambiguous bot names`, killing the whole run. Sub-entries renamed `lineage.py`.
- **Mixed-team bug:** on bridge, Core and Builders picked vanguard but four field-built Launchers (vision r²=26, no Core in sight) picked tempest — two incompatible store layouts on one team; the initial 2-8 closed-map result measured that bug. Fix: verdict travels via store slot 13 (vanguard never touches it; tempest writes only packed positions ≤958, so a large sentinel is unambiguous). After the fix janus-on-closed reproduces standalone vanguard exactly (4-6).
- **CPU:** importing both lineages costs 30–55 ms but lands at Player construction, outside the 10 ms turn budget; peak turn 1.94 ms (vs prospect 0.94 ms).
- **Why the predicted gain vanished:** `tempest@da3fd8a` vs current vanguard on the five corner maps: **vanguard 7-3** (the tournament's prediction was right); current prospect vs `tempest@da3fd8a`: **prospect 9-1**; current prospect vs current vanguard: **prospect 6-4**. The tempest line had simply outgrown the measurement. Lesson recorded to memory: **"a map-strategy result is a fact about two particular bots on a particular afternoon, not a fact about the maps. Mine had a shelf life of roughly one day of your commits."** janus committed and pushed as `8a1e7a1ec` — "because it's correct, cheap to re-measure, and the plumbing is the hard part… not because it wins." Local pytest 8 failed / 75 passed, byte-identical to baseline.

### 3e. Ladder outage and evaluator fix (14:17–15:01)
Lucas, 14:17: "Why are none of the bots on the website, did you not push?" Claude first checked the wrong website — the official league platform (`fcode submit`; ladder entries all uploaded by Jonathan: v5 "Tempest Fast" active, v4 "Undertow", v3 "Tempest"; deliberately never submitted since "each submission replaces your current ladder entry"). Lucas corrected: "No I meant my lucasrgpedersen.com/botranking. Look in the agents.md in x/tournament… And note this was made into a CI workflow."

Diagnosis: the evaluator (`botrankings-evaluator.timer`) was discovering the bots fine every tick (`x/luc@8a1e7a1: 5 bot(s) under bots/luc`) but **crashing at publish 18 ticks in a row since 15:31 local (13:31 UTC)**: `RuntimeError: refusing to publish an incomplete matrix -- 9 pair(s) have never played`. Root cause: bot ids are `name@<branch-tip-sha>`, so **every push to `x/luc` re-mints an id for every bot on the branch** (`vigil@1b5f3d1` existed purely because Claude's commit moved the tip). Claude's push `1b5f3d1b8` and Lucas's own `bf9b3e374` (his in-flight vigil launcher-breaker work, committed at 15:21 local) landed ~10 minutes apart; two challenger runs (`auto-de5c5502bec1`, `auto-66018bc1277c`) were planned back-to-back, and since `canonical_field()` only contains bots that have already played, run 2 was finalised against a field it was never planned against. The guard worked as designed; the race was upstream.

Lucas: "Gap filling should be fine." → Planned `gapfill-idrace-20260802`: **504 matches** (9 missing pairs + 3 that dedupe via content-addressed `match_id`), ran locally at `--jobs 8`, **0 errors**. Matrix complete: **83 bots, 142,909 matches**. Next tick published: "deployed 4680fc4 to Cloudflare" at 16:37:25 local; a second deploy `146b097` at 16:56:39 local. Timer pause/resume logged in the system changelog. Resulting ranking: **#1 `vigil@18b749d` 0.9288 (Nash core agent, prob 0.5)**, #3 `vigil@1b5f3d1` 0.9119, #4 `vigil@bf9b3e3` 0.9059, #6 `prospect_rushonly@bf9b3e3` 0.8159, #7 `prospect@bf9b3e3` 0.8261. The doctrine A/B finally measured at scale — **3,444 games each: prospect 0.8252 vs prospect_rushonly 0.8159 (+0.9pp)**, but mElo ranks them the other way (2.211 vs 2.233), so "small positive, not confirmed win." Head-to-head reproduced local numbers exactly (8/10 closed, 16/32 mirror on open).

Lucas: "Yeah just fix it, and afterwards ensure the change didn't break anything else since two other agents are working beside you." → Fix pushed as **`b7bcede16` on `x/tournament`** (worked in a temporary worktree to avoid editing under the live evaluator): (1) evaluator now **plans one run at a time**; `unseen` is re-derived from match data each tick so deferred work gets planned after the in-flight run publishes; (2) in-flight bots are resolved to the code they *are*, not whatever sits at their id. Both regression tests fail on the parent commit; suite 131 → 133. Evaluator self-updated and the next tick exited success in 4.9 s CPU (from a 2h19m tick). janus's own challenger run `auto-7202408d73d7` (3,507 matches) went live on the cluster. Verified the other agents' in-flight work untouched (`bots/luc/ragnarok/`, `benchmarks/`, their vigil edits); pre-checked `ragnarok` against the discovery hazard (one `main.py`, no name clash — "safe to push"). Stated caveat: deferring runs costs latency — "nothing is being scheduled" now means "check for a stuck run."

**Names:** `prospect` — fair vigil fork with round-0 cornered-Core doctrine. `prospect_rushonly` — its feature-off A/B twin (discovered as a real bot by the harness). `janus` — round-0 dispatcher bundling prospect and vendored vanguard lineages. `vanguard_oracle`, `tempest_oracle_guarded` — existing roster bots the k=2 branch analysis picked. `BLD4`, `e_*` family — other atlas-carrying bots. "Tempest Fast" / "Undertow" / "Tempest" — the team's official-platform ladder entries, all uploaded by Jonathan.

---

## Session 4 — 797291d9-9e2d-4850-a741-98533f9b4049 (13:27 `/clear`; work 14:26:32–20:30:20 UTC)

**Goal (Lucas's prompt, 14:26, verbatim core):** "You are the final boss of this whole competition… build the absolute best bot possible based on all the learnings we have made along the way. Use the branch x/tournament for data. Make a detailed benchmarking on different tasks in this game… I have a hypothesis that our bots are currently beating each other sure, but for individual mechanics/specific code we might be going in circles, regressing… Make an analysis of this hypothesis and write it in the repo. Then, make a completely superior bot to everyone else, taking the best code from every single bot…"

Model churn shaped this session: set to `claude-fable-5[1m]` at 14:26; hit the monthly spend limit ~14:50; "out of usage credits" 15:41; switched to `opus` (claude-opus-5) 15:42; session limit at 15:59 and again 16:15 (resets 9:10pm Copenhagen); resumed 16:11 and finally 19:20.

### The benchmark package and the hypothesis
Built `benchmarks/` — decodes `.replay26` into per-round events (a parser bug was found and fixed: in the core snapshot, field 1 is entity id and field 2 is team, absent = A) and runs five suites: expansion vs an idle probe, a denial probe that attacks everything except the Core, breaching, defending, and a 12-bot round robin. Full field run: **1,944 matches, zero errors**. Verdict: **"Your hypothesis was right, and understated"** — three lineages deleted a solved mechanic outright:
- `mistral` built **0 harvesters in 138 games**; `tempest` built 1 — their own ancestors delivered 5,700+ titanium; that forfeits all 146 tiebreak games by construction.
- The bots *built to defend* lose to rushes worse than the rush bots do: vanguard 2/10, undertow 1/10, casemate 0/10 vs the attackers' 7/10.
- `casemate` pairs a genuinely new weapon with an 8-connected pathfinder on a cardinal-only engine — 868–975-round builder stalls, `titanium_collected = 0` in all 40 of its games.
- Sharpest per-map evidence: an economy-free bot scores **21/22 on showdown and 6/22 on quarry**; the economy bot is the exact mirror; neither lineage carries both. Write-up committed at `benchmarks/ANALYSIS.md`.
- Also verified by arithmetic: shipped `prospect` has a store-packing bug — `pack_core` writes the doctrine bit into bits 10+ of `SLOT_OWN_CORE`, so any consumer using plain `unpack_pos` reads x **off by 32 tiles on every FORTIFY map** (e.g. core (9,0) doctrine=1 unpacks as (41,0)).

### ragnarok
`bots/luc/ragnarok` — assembled from the best measured components across bots; branched from Lucas's uncommitted vigil working state (vigil at `e22eda8d6`). **Every mechanic ablated rather than argued** (1,080 games, shipped bot vs itself with one change reverted, 6-bot panel): field Gunners under FORTIFY **+8**, lane rebuild-tanking (lane-blocking) **+5**, corner doctrine **+5**, piercing-Sentinel siege **+2**; three measured **exactly zero** (Launcher retirement, second trunk, ore denial) — kept but documented as measuring nothing. Two things measurement overturned: vigil ships field Gunners *off* at −3 pool-wide, but conditioned on closed maps they're the single biggest win; and the BLITZ doctrine threshold — Chebyshev core-distance ≤8 is where the pool's distances have a natural gap (selects showdown/sprint/duel) but measured *worse than off* because 8 includes duel at −6; **shipped value 6**. BLITZ kills showdown at turn 18. The atlas measured worth **63 games**; per CLAUDE.local.md constraints the atlas import auto-flags a bot unfair via `tournament/fairness.py`, so both variants shipped:
- **ragnarok: 322/378 (85.2%)** over all 21 maps, both seats, nine opponents — beats every bot it was assembled from (vigil 26-16, tempest_reinforcements 28-14).
- **ragnarok_fair: 70.6%**, generated deterministically by `tools/build_ragnarok_fair.py` (fails if an `atlas` import survives), audited for any pool-derived knowledge beyond tuned constants; loses only to the three atlas-carrying bots.

Committed on `x/luc`; pushed only after Lucas's "Push your changes" (19:24) → `e22eda8d6..34f775b5f`. `benchmarks/runs/` gitignored because staged bot copies contain `main.py` (the discovery-duplicate hazard); probes named `entry.py`.

### The TLE fix (20:18–20:30)
Lucas: "Right now it goes over time unfortunately." Reproduced with new `benchmarks/timing.py` (compliance-style wrapper reading the engine's own clock): **longship worst turn 13,480 µs** against the 10 ms budget, all Builder turns (p50 294 µs, p99 7,087 µs), worst map 9× the cheapest. Cause: `_keeps_route_open` runs up to two full BFS per candidate *inside a 196-candidate loop* — one Gunner siting could search the map up to 392 times. Two exact (not sampling) fixes: only sites on the current route need the second search, and candidates are ranked first so the check is paid only until one survives. Result: worst turn **13,480 → 2,995 µs**, p99 7,087 → 2,273 µs, overruns 2 → 0, whole field 3.4× under the limit; plus a hard CPU guard. Behaviour-preservation proven: first comparison showed −3 games, all 35 changed games vigil matchups — because **vigil changed on disk mid-run** (Lucas was editing it in parallel); against a frozen vigil, **42/42 byte-identical games**. Pushed `e267eeb41..79582fccd`.

**Open decision handed to Lucas at close:** Lucas committed `2956db378` and `e267eeb41` to vigil in parallel — including independently making the same route-search optimization — so ragnarok is missing four vigil fixes (stuck-Builder stand-down, conveyor-line repair, blocked-Builder Launcher escape, ring Builder maintaining the line instead of idling 984 rounds). Ragnarok beat the older vigil 26-16 but beats current vigil only 23-19. Port offered (~230-line real merge), not yet done.

**Names:** `ragnarok` — the "final boss" composite bot (atlas, flagged unfair by design). `ragnarok_fair` — generated atlas-free sibling. `casemate` — Jon's bot with a novel weapon but broken 8-connected pathfinding. `gobbleglitch` — bot whose structure was studied for components. BLITZ — ragnarok doctrine: all-in rush when cores are within Chebyshev distance ≤6. FORTIFY — closed-map doctrine (field Gunners, lane play). `benchmarks/` — the per-mechanic benchmarking package (suites + replay decoder + timing.py).

---

## Session 5 — 0f3c24e1-aef7-4d65-af74-01aa89843891 (15:56 `/clear`; prompt 15:58; work 19:20:09–20:34:38 UTC)

**Goal (Lucas, 15:58):** "On the website (~/projects/portfolio) it seems that the buttons selecting for only fair bots doesn't actually select for only fair bots. Can you 1. make sure it works properly and 2. doublecheck that each combination of fair bots and include overtime strategies (4 combs) have distinct nash average and melo_r scorings?" (Hit session limit immediately; resumed 19:20 with "continue".)

**Narrative.** The bug was server-side, in `tournament/site_data.py:283` (florent-code-league-ci): the fair field was built with a tag-only test (`if "unfair" not in …tags`) while the badge on each row uses `is_unfair(tags, commit, path)`, which also flags bots importing a bundled offline map atlas. Result: `rankings_fair` shipped **53 bots of which 6 unfair**, `rankings_fair_including_over_time` **60 with 7 unfair**, and every fair rating was computed partly against them. Fixed to use the same `is_unfair()` predicate; after regeneration: 47 bots / 0 unfair and 53 / 0 unfair, verified across all 42 map-file variants. Also fixed the UI label (BotRankings.tsx:604) hardcoded to "(17)" regardless of scope (fair-only adds 6). Consequence: **the fair field now has a different champion — `prospect` — than the full field (`vigil`)**.

Distinctness: `melo_r` fully distinct across all 6 variant pairs (max deltas 0.13–0.67). `nash_average` has identical values that are mathematically correct, not copies: 18 of 25 base↔over-time coincidences are exactly `-4.442651` (the smoothing-clipped log-odds floor for bots losing 100% to every Nash-support opponent); `_fair` vs `_fair_including_over_time` agree on all 47 shared bots (maxdiff exactly 0.0) because both have a *pure* equilibrium on `prospect` and all 6 added over-time bots carry `nash_prob = 0.0` — adding dominated strategies leaves Nash averages untouched. Base field equilibrium: 50/50 `vigil@18b749d` + `tempest_reinforcements`; over-time: pure `vigil@e22eda8`.

**Second round (20:22), Lucas's questions:** (1) four bots show nash average 0 — three `+0`, one `-0` (`vigil@e22eda8`, not marked Nash core) — what's the difference? (2) can tied Nash ranks share a rank ("the nash core… should all be called 1"), and what's the secondary sort — "I would think mElo_r would make sense but what's your take?"

Answers (run `auto-52c8e804e94c`, over-time included; the automation had rerun at 19:48 local while working, so the field moved — `ragnarok`/`ragnarok_fair` were by then in the Nash core): ragnarok `0.0` (★ p=0.2489), ragnarok_fair `0.0` (★ p=0.2489), `vigil@2956db3` `-0.0` (★ p=0.5022 — IEEE-754 signed zero, meaningless), **`vigil@e22eda8` `-5e-06` (p=0)** — genuinely negative, genuinely excluded from the core, but `(-0.000005).toFixed(4)` printed `"-0.0000"`: "the site was telling you the opposite of the truth." Fixed `signed()` in BotRankings.tsx:167 — exact zeros normalize to `+0.0000`; sub-precision values render in exponent form (`-5.0e-6`). Implemented competition ranking (1, 1, 1, 4) in `tournament/report.py:41-55`, with ties judged at **6 decimals — the precision the row publishes** — because at 9 decimals the core still split on solver noise (`0.0` vs `-3e-9`). On the secondary sort: it already *is* `melo_r` (`lexsort` key `(-ratings.transitive, -nash_average)`), and Claude endorsed it — Nash measures unexploitability, mElo's transitive term measures raw strength, the orthogonal axis — but insisted the tiebreak decide **display order, never rank**: "Ranking them 1-2-3 by melo_r invented a claim the math doesn't make." 44 tests pass, `tsc --noEmit` clean. **All changes left uncommitted across both repos at session end.**

**Names:** `is_unfair()` — the harness predicate (tags + atlas-import detection) that defines fairness on the site. `auto-52c8e804e94c` — the run current at the time. Competition ranking — ties share rank, next rank skips (1,1,1,4).

---

## Chunk synthesis

Load-bearing insights of 2026-08-02, with evidence:

1. **The biggest single-day performance jump came from a bug neither hypothesis pointed at:** tempest_reinforcements played whole matches at 0–1 ammo, gating all four gunner-build paths (`MIN_AMMO_FOR_GUNNER = 20`). Adding `COMBAT_AMMO_FLOOR = 80` took it 52.4% → 78.6% (+22/−0 paired, p<0.00001), pushed as `65ddc5545`. Both original suspects (titanium reserve, lane gate) were measured near-inert: 0 and 1 flipped cells out of 84.
2. **Engine seeds are completely inert** — 3 seeds reproduced identical results exactly (63-63 = 3× the 21-21 seed-1 result); real variation is map × side only. All power calculations should count 21 maps × 2 sides per opponent.
3. **The closed/open map split is real, huge, and validated:** identical 4-cluster partition on independent match halves (adjusted Rand 1.00); rush wins 81.9%/79.4% on open groups C/D vs 27.9%/24.7% on closed A/B; bridge 3.6% vs showdown 96.9% for the same bot pair. Geometry predicts it (wall fraction r=−0.65).
4. **One strategy branch captures two-thirds of the map-awareness ceiling; more branches are near-worthless:** atlas-free out-of-sample WR 60.7% (k=1) → 67.5% (k=2, +6.8pp) → 66.9% (k=4, worse) → 71.0% (per-map oracle, 12 bots). The forced geometric split matters — free clustering yields only +1.7pp.
5. **The atlas exclusion is a house rule, not a league rule** — the league only says maps are "drawn at random from the current competition map pool… announced at the start"; and turn-0 info identifies a known map but cannot measure its geometry (core vision r²=36, no terrain getter) — the exact gap the atlas fills. Measured atlas value in the ragnarok panel: 63 games (85.2% vs 70.6%).
6. **A knob-flip cannot reproduce a between-bot difference:** prospect's fortify doctrine won 8-2 vs its own twin but was a wash (6/10 = 6/10) vs real vanguard; tempest ~5.6 buildings/~90 Ti vs vanguard 15-19/500-650 vs undertow ~20/~950 are "different machines." Lucas diagnosed this himself ("vastly different strategies won different maps"). At full-field scale the doctrine measured +0.9pp win rate (0.8252 vs 0.8159 over 3,444 games each) with mElo disagreeing (2.211 vs 2.233).
7. **Map-strategy results go stale in about a day of commits:** the tournament's "vanguard beats tempest on closed maps" (7-3) was true against `tempest@da3fd8a`, but current prospect beats that pinned tempest 9-1 and current vanguard 6-4, so janus's vanguard branch no longer pays. (Recorded to memory; janus kept as cheap-to-remeasure plumbing.)
8. **Game-engine facts established by probing:** units do NOT share module globals (each unit has its own namespace; the 16-slot store is the only cross-unit channel); store writes are invisible to other units until the next round; unit visions r² — core 36, launcher 26, builder 20, gunner 13; a field-built unit may see no Core at all (janus's mixed-team bug: 4 Launchers on bridge picked the wrong lineage).
9. **Tournament discovery treats any directory with `main.py` as a bot** — nested entry points cause `ValueError: ambiguous bot names` and kill the whole run. Mitigations now conventional: sub-entries named `lineage.py`, benchmark probes named `entry.py`, staged copies gitignored.
10. **Bot ids keyed on `name@branch-tip-sha` re-mint every bot on every push**, and `canonical_field()` grows as runs land, so two pushes ~10 min apart wedged publishing for 18 ticks (RuntimeError, 9 never-played pairs). Fixed by gap-fill (`gapfill-idrace-20260802`, 504 matches, 0 errors → 83 bots / 142,909 matches complete) then durably by `b7bcede16` on `x/tournament`: plan one run at a time + resolve in-flight bots to their code. New symptom to know: "nothing scheduled" now means "check for a stuck run."
11. **Lucas's mechanic-regression hypothesis was right "and understated":** lineages deleted solved mechanics outright — mistral 0 harvesters in 138 games (ancestors: 5,700+ Ti; forfeits all 146 tiebreak games), defensive bots lose to rushes worse than attackers do (vanguard 2/10, undertow 1/10, casemate 0/10 vs 7/10), casemate's 8-connected pathfinder on a cardinal-only engine stalls builders 868–975 rounds with 0 titanium in all 40 games. Evidence: `benchmarks/` (1,944 matches, zero errors), write-up in `benchmarks/ANALYSIS.md`.
12. **Ablation beats argument:** of ragnarok's adopted mechanics (1,080-game one-change-reverted runs): field Gunners under FORTIFY +8, lane-blocking +5, corner doctrine +5, Sentinel siege +2, and three exactly zero. Two priors overturned by measurement: vigil ships field Gunners off (−3 pool-wide but biggest win on closed maps); the "natural gap" BLITZ threshold 8 measured worse than off (duel −6) — shipped 6.
13. **ragnarok is the day's strongest bot: 322/378 (85.2%)** vs nine opponents on all 21 maps, beating everything it was assembled from (vigil 26-16, tempest_reinforcements 28-14); `ragnarok_fair` (generated, atlas-free) 70.6%, losing only to the three atlas bots. Pushed `34f775b5f`, then TLE-fixed and pushed `79582fccd`.
14. **A TLE is a correctness bug, not slowness:** an over-10ms unit is interrupted and silently skips its turn. ragnarok's `_keeps_route_open` (2 BFS × 196 candidates ≈ 392 map searches per Gunner siting) hit 13,480 µs on longship only — worst map 9× cheapest, so small map samples can't certify compliance. Exact fix: 2,995 µs worst, 0 overruns; proven behaviour-preserving by 42/42 byte-identical games vs a frozen opponent.
15. **Frozen opponents are mandatory for A/B:** the first before/after comparison showed −3, entirely because Lucas edited vigil on disk mid-run (`2956db378`, `e267eeb41` — independently making the same route-search fix).
16. **The site's "fair" filter was leaking unfair bots** (tag-only test vs the `is_unfair()` atlas-import detector): 6 unfair bots inside `rankings_fair`, 7 in the over-time variant; fixed in `site_data.py`, after which **the fair champion is `prospect` while the full-field champion is `vigil`**.
17. **Nash-score display subtleties:** the value `-4.442651` is the smoothing-clipped floor (100% losses to all support opponents); identical nash_average between fair variants is correct when the Nash support is unchanged (pure equilibrium on prospect, added bots at nash_prob 0.0); `-0.0000` on the site was a real bug hiding `-5e-06` (a genuinely-exploitable non-core `vigil@e22eda8`) while true signed zero `-0.0` (`vigil@2956db3`, core p=0.5022) was harmless. Ties now share rank (1,1,1,4) at 6-decimal published precision; melo_r stays as display-order tiebreak only.
18. **Lucas's stated preferences this day:** explain plainly, no invented shorthand ("It feels like you are speaking in code… you never introduced me to two bots"); test hypotheses empirically against named pinned opponents; pushing is his call (it triggers the full tournament); protect the CI ("just check that it won't confuse the CI"); fix infrastructure when found but verify against parallel agents' work; ties in the Nash core should all rank #1.
19. **Ladder/infra facts:** the official league platform is separate from git — entries go up via `fcode submit` and *replace* the team's active entry (all current entries uploaded by Jonathan: v5 "Tempest Fast" active); the git remote is `github.com/jonathantybirk/florent-code-league`; the personal ranking site is `lucasrgpedersen.com/botranking`, deployed to Cloudflare Workers from `~/projects/portfolio`, fed by `tournament/site_data.py`; the portfolio's `buildInspectionPrompt` button generates replay-inspection prompts for Claude sessions (fixed `895dcec`: `--replay` over blocking `--watch`, disambiguated repo root).
20. **Ranking snapshot after the 14:33 UTC gap-fill:** vigil@18b749d #1 at 0.9288 (Nash core, prob 0.5); three vigil generations in the top 4; prospect/prospect_rushonly at 0.8261/0.8159. By evening (run `auto-52c8e804e94c`, after ragnarok's arrival) ragnarok and ragnarok_fair were both Nash-core members at p=0.2489 each with vigil@2956db3 at p=0.5022.

### Loose ends at chunk close
- **Port of Lucas's four vigil fixes into ragnarok** (stuck-Builder stand-down, conveyor-line repair, blocked-Builder Launcher escape, ring-Builder line maintenance) — offered, not done; ragnarok's edge over current vigil narrowed 26-16 → 23-19.
- **Prospect's `pack_core` off-by-32 bug** (doctrine bit corrupts `unpack_pos` x by +32 on every FORTIFY map) — verified and documented in the ragnarok session's analysis, but no fix to prospect itself was recorded this day.
- **Portfolio + report.py changes (fair filter, signed(), tied ranks) left uncommitted across both repos** at 20:34.
- **10 replay-visualizer servers (plus one stale) left running** — Lucas never said to kill them.
- **janus's challenger run** `auto-7202408d73d7` was in flight at the cluster; its rating had not appeared yet.
- **prospect_rushonly is on the ladder as a real bot** — flagged for Lucas to decide whether a measuring stick belongs in the ratings.
- **ragnarok/ragnarok_fair's full-tournament rating pending** — 85.2% was against a self-chosen nine-bot panel; the ladder field includes Elias's and Jon's probe families and `vg_*` ancestors.
- **The lane gate from `51ad530`** left in tempest_reinforcements — "could still be costing you against opponents that fight differently than these two."
- **Evaluator's new failure symptom** — with one-run-at-a-time planning, a stuck cluster run now silently queues new bots behind it.
